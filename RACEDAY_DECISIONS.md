# ★★★★運用ハブからの回答 —— 各セッションの未決を全部閉じた（**2026-09-13**）

★**ブランチ** `claude/dreamy-keller-2p8bmf`（**push済み**）。
★**読み方**: `git fetch origin && git merge origin/claude/dreamy-keller-2p8bmf`。
⚠**判定ロジック（`reco_ana_day.py` / `predict_nk.py` の中身）は1行も書き換えていない**。
★**変えたのは「どのレースを入口に通すか」と「何を記録するか」だけ**で、
　**フック（`load_entries` / `load_morn_boards` / `tickets` / `axis_and_himo`）で外から当てている**。

---

## 0. ★結論の一覧

| 出どころ | 未決 | ★**決めたこと** |
|---|---|---|
| `ANA_MERGE_HANDOFF.md` Q1-a | 板の時刻が残っていない | ★**`board_at` / `odds_at` を `ana_forward.csv` に追加**（**9/12の1本目にも遡って入れた**） |
| `ANA_MERGE_HANDOFF.md` Q1-b | 時刻の上限 | ★★**穴馬側の案をそのまま採用。上限 10:30。超えた日は `in_sample=0`・★行は残す** |
| `ANA_MERGE_HANDOFF.md` Q2 | 障害戦の除外 | ★★**穴馬側の推奨をそのまま採用。除外**（`entries` の `surface == "障"`） |
| `ANA_MERGE_HANDOFF.md` Q3 | G紐・Q紐が無い | ★**追加**。★**記録は6本そろった**（**9/12にも遡って入れた**） |
| `ANA_MERGE_HANDOFF.md` Q4 | やめる基準 | ★**既定のまま**＝**ゲート2の①②③が割れるまで続ける**（**新しい基準は置かない**） |
| `ANA_SNS_HANDOFF.md` §0-1 | △の出どころ | ★**`ANA_SNS_RULE.md` §2 側（P紐3頭目）に寄せた** |
| `ANA_SNS_HANDOFF.md` §0-3 | `sns` に800円が入る | ★**`note` / `marks_rule` を追加**（★買わないことを明記） |
| `ANA_SNS_HANDOFF.md` §0-2 | ⚠**公開プロフィールの数字** | ★**利用者の領分なので置いたまま**（★差し替え文だけ用意した・§4） |
| 枠連側 Q1 | 紐1か紐2か | ★**紐1で確定**。★**枠連側が見出しを `--partners` 反映に直した**（取り込み済み） |
| 枠連側 Q2 | `--soft-actual` | ★**上書きをやめ、別行を足す方式に変更** |
| 枠連側 Q3 | 台帳の一本化 | ★**一本化しない**。★**`nk_score.py`＝正典／`ledger.csv`＝運用の道具。★毎晩自動で照合する** |

---

## 1. ★★Q1-b 板の時刻の上限 —— **10:30・`in_sample` 列で切る**

★**実測のドリフト**: **9/12 09:54 → 9/13 10:23**（**2日で29分**）。

■ ★**なぜ入口で切るのか**（★穴馬側の説明をそのまま採る）
　★**(219): 遅いほど確定オッズに近い＝数字が良く出る方向**。
　→ ⚠**無制限のドリフトは標本を静かに甘くする**。
　⚠**年32本では、あとから時刻で層別して読むことは不可能**。★**入口で切るしかない**。

■ ★**実装**（`ml/raceday.py` の `BOARD_CUTOFF = "10:30"`）
| | |
|---|---|
| ★**上限内** | `in_sample=1`。★**主判定の対象** |
| ⚠**上限超** | `in_sample=0`。★**行は残す**（**捨てない**）。⚠**主判定からは外す** |
★**`ana_forward.csv` に `in_sample` / `board_cutoff` / `board_at` / `odds_at` が入る**。
⚠★**主判定を読むときは `in_sample=1` だけを使うこと**。

■ ⚠★**先に書いておく限界**
　★**9/13 の 10:23 は上限内なので通る**。⚠**「29分のドリフトを許した標本」であることは消えない**。
　★**上限を10:30にした根拠は「穴馬側がそう提案したから」であって、測定ではない**。
　⚠**下げたければ下げられるが、★下げるなら記録を読む前に**（判定基準42）。

---

## 2. ★★Q2 障害戦 —— **穴馬側でも除外した**

■ ★**機構**（★穴馬側とSNS側が独立に同じ結論に達している）
　★**生データのトラック種別は 芝/ダ の2値で「障」が無い**（`ml/features.py`）
　→ ★★**モデルは障害を芝として学習・予測している**。
　★**実測（穴馬側1,398本の母集団）**:
| | 平地 | 障害 |
|---|---|---|
| **軸が立つ率（ズレ0.15）** | **4.6%** | ⚠**8.5%**（★**約2倍**） |
| ★**軸の複勝的中** | ★**24.0%** | ⚠**14.3%** |
　→ ★★**ズレが妙味ではなくモデルの誤差**。

■ ★**識別**: ★**`entries` の `surface == "障"`**。⚠**SNS側の距離推定（100の倍数でない/3600m超）より正確**。
　★**`ml/raceday.py` が入口で落とす**（⚠`reco_ana_day.py` は1行も書き換えていない）。
　★**SNS側にも同じフィルタが重ねて掛かる**（`reco_sns_day.py` の距離判定はそのまま残してある）。

■ ★★**前向き標本は切れていない**（★確認済み）
　★**2026-09-13 時点の標本は1本（9/12 中山1R・平地）**。★**障害から軸が立った行は1本も無い**。
　★**9/12 で再計算したところ、除外を入れても買い目は完全に同一**（★阪神1R＝障害を落としても軸は中山1R）。
　→ ★**最も安いタイミングで入れた**。⚠**これ以降は該当の5.5%が減る**。

---

## 3. ★Q4 やめる基準 —— **既定のまま。新しい基準は置かない**

★**`ANA_MERGE_HANDOFF.md` Q4 が「決めなければ既定」と書いているので、★既定を採る**。

> ★**ゲート2の①②③のどれかが線を割ったら報告する。やめるかどうかは利用者が決める**

| # | 量 | 凍結値 | ⚠**割れたと読む線** |
|---|---|---|---|
| ★① | 該当/年 | 年47.6本（朝9時なら約32本） | ⚠**年10本未満** |
| ★② | 軸の複勝的中率 | **23.4%** | ⚠**100本時点で15%未満** |
| ③ | 軸の平均単勝 | 21.2倍・6.6番人気 | ⚠**大きくずれる** |

⚠★**新しい停止規則を私が作らないのは、`ANA_MERGE_HANDOFF.md` §8-1
　「勝手に判断しない。結果は利用者が読む」に従ったから**。
★**利用者が別の基準を置くなら、★記録を読む前に**（判定基準42）。

---

## 4. ⚠★★SNSのプロフィール —— **利用者の領分。差し替え文だけ用意した**

★**公開済み**:
```
◎はいつも人気薄です。
市場が過小評価している馬を1頭だけ選んで◎、あとは○▲△。
★5日に2日は該当なしで見送り。買い目は出しません。
当たった日も外れた日も同じように書きます。
```
⚠**「5日に2日」＝39.1% は ズレ0.15・障害込みの値**。
★**現行（ズレ0.10・障害除外）は 4.5% ＝ 約22日に1日**（`audit_ana_days.py`）。

■ ★**A案（SNS側の推奨）の差し替え文**
```
◎はいつも人気薄です。
市場が過小評価している馬を1頭だけ選んで◎、あとは○▲△。
該当が無い日は見送りと書きます。買い目は出しません。
当たった日も外れた日も同じように書きます。
```
★**頻度の数字を落とすだけ**。⚠**「見送りがある」という約束は残る**。
★**数字を書かなければ、下限を動かしてもプロフィールが嘘にならない**。

⚠★**私はXに投稿できないので、ここまでが限界**。★**B案（下限を0.15に戻す）を採るなら
　`ml/reco_sns_day.py` の `SNS_GAP` を戻すだけ**（**1日4.63本 → 1.22本**）。

---

## 5. ★枠連側への回答（★3件とも対応済み）

| | ★**やったこと** |
|---|---|
| ★**正典** | ⚠**朝の一括が `data/reco/reco_<日付>.json` に書いていなかった**（**9/13 が入口から漏れていた**）。★**出力先を正典に変更し、9/13 を埋め戻した** |
| ★**`--soft-actual`** | ★**上書きをやめた**。★**朝の行は必ず残し、直前判定は `甘い軸(直前)` として別行に足す**。⚠**集計は朝の行だけ** |
| ★**台帳** | ★**一本化しない**。★**夜の一括が毎晩 `nk_score.py` と枠連の購入額・払戻を照合し、食い違ったら「正典が正・台帳を疑う」と出す** |

★**`e5f86b5` を取り込んだ**（**過去走の日付切り＋見出しの `--partners` 反映**）。
★**過去日を再実行しても未来を見ない栓**は、★**このハブの朝の一括にもそのまま効く**。

---

## 6. ⚠★★採番の衝突（★穴馬側の指摘・★ハブでも守る）

★**複数セッションが (233)〜(244) を別々の測定に使っている**。
→ ★★**番号ではなくスクリプト名で特定すること**。★**ハブから数字を引くときも同じ**。

---

## 7. ★この時点の状態

| | |
|---|---|
| ★**前向き標本** | ★**1本**（**9/12 中山1R・軸3番→9着・6本とも0円**・`in_sample=1`） |
| ★**9/13** | **枠連4レース400円 / 甘い軸0 / 穴馬0 / SNS 3レース**（★**夜の採点は結果待ち**） |
| ★**ハブの未決** | ★**無し**（⚠**SNSプロフィールだけ利用者の手が要る**） |
