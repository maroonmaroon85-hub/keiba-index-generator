# 別セッション用の引き継ぎ 第2版 — **候補は3つ。まずどれをやるか決めてから始める**

**2026-09-06 作成**。★**このファイルだけで始められるように書いてある**。
⚠**まず `HANDOFF.md` の「★判定基準」（1〜46）と `ROI_MAP.md` を読むこと**。**そこが本体**。

---

## 0. 役割分担（★これを守る）

| セッション | 担当 |
|---|---|
| **元のセッション**（`claude/handoff-env-check-2kexpo`） | ★**枠連の日々の運用**（出馬表→推奨→結果→記録）と **(148) 時系列オッズ** |
| ★**このセッション** | ★**下の候補から1つだけ**。**運用には手を出さない** |

⚠**運用の設定（紐1・除外40%・枠連）を変える提案はしない**。**測ったら元セッションに渡す**。
★**このセッション用に新しいブランチを切ること**。
⚠**`git add` の直前にもう一度 `git pull`**（元セッションが同じリポジトリに push している）。

---

## 1. ⚠まず読む — **この2つの線はもう閉じている**

| 線 | 状態 | 置き場 |
|---|---|---|
| ★**穴馬のオッズ妙味**（(174)〜(205)・32本） | ✅**完了** | ブランチ `claude/determined-allen-2s07uf` の `ANA_RESULT.md` |
| **枠連の比の裾**（(141)〜(173)） | ⏳**(148)待ち**。★**元セッションの担当** | `HANDOFF.md` / `ROI_MAP.md` |

★**穴馬の答え**: **妙味は実在する（同じオッズ帯の馬より複勝で +5.8〜+17.9円/レース・11年劣化なし。
　ズレが大きいほど優位も大きく5帯すべて単調）。⚠だが最高97.3%で、券種を変えても
　「必要量の80〜82%」で頭打ち**。
⚠⚠**この結論を他の線に持ち込まないこと（判定基準25）**——
　**q=MLモデル / q_pool=複勝の板 の弱い経路**であって、
　**枠連(141)の「馬連プール→枠（板の恒等式に乗った厳密な変換）」とは別物**。

---

## 2. ★★候補（**3つ。始める前にユーザーに1つ選んでもらう**）

### ◇候補A — **除外も券種ごとに分ける**（ROI_MAP ◇24d）

★**(169)から見えた設計**。**枠連は top2 の `waku_score`、三連複は top3 の基準で除外する**。
★**なぜ有望か**: **(168)で「top2で学習すると枠連が+1.3pt」と出たが、
　(169)で分解したら +1.1pt は「除外の判定」から来ていた**（**買い目の目標の効果は+0.2ptだけ**）。
　→ ★**効いていたのは除外のほう**。**なら除外を券種ごとに分けるのが素直**。
⚠**事前登録し直してから測る**。**(169)の結果を見てアームを足したことになるので、
　前半で選び後半で1回だけ試す形にする（判定基準46）**。
⚠**運用に触る提案になりうる**。★**測るだけにして、採否は元セッション（＝ユーザー）に返す**。

### ◇候補B — **ワイドの板 (type=5) を集めて6マスの表を7マスにする**（保留3）

★**現在の表（強い経路のROI − その券種の払戻率）**:
　**枠連 +38.5pt / 複勝 +9.8 / 単勝 −10.2 / 馬連 −34.5 / 馬単 −36.3 / 三連複 −46.7**
⚠**判定基準40により「枠連は孤立した事実。説明変数は無い」を採用済み**。
　→ ★**ワイドを足す意味は「40の判断を1マスで検算する」ことだけ**。**新しい説明を探すためではない**。
⚠**収集は Mac で約20時間**（`python3 ml/nk_odds_bulk.py --type 5`）。
⚠**見積もりは3回外している**（馬単「数時間」→20h / 三連単「20〜25時間」→48h）。**規則には上限がある**。
★**優先度は低い**。**1マス増えても40の判断はたぶん変わらない**——**先にそう書いておく**。

### ◇候補C — **穴馬の線の未測定2件**（`ANA_RESULT.md` §6）

⚠⚠**どちらも再開条件つき。条件を満たしていないなら開けない**。

| # | 内容 | ★**再開条件** |
|---|---|---|
| 1 | **混合 w=0.75 の複勝 98.5%**（買う率13.7%・この線の最高値） | **別の年で前半検証を繰り返し、2期以上で現行を上回ったとき** |
| 2 | **三連単** | **的中率を1桁上げる買い方が見つかったとき、または分散の小さい代理判定量を思いついたとき** |

★**1をやるなら「別の年での前半検証」がそのまま作業**。**混合そのものを測り直すのではない**。

---

## 3. ★★測り方（**判定基準43〜46。前の線で高くついて学んだもの**）

★**43 無作為対照は1回引きでは足りない**
　**乱数の種を変えるだけでROIが 82.9〜97.4% 動いた**（標準偏差3.8pt・マスによっては8.2pt）。
　⚠**これで結論を1つ誤った**（(194)「後半も+4.6円勝っている」→20種平均は−0.8円・10/20種でしか正でない）。
　→ ★**乱は10種以上の平均**。**種はレースに紐づけ、ループ順に依存させない**。

★**44 裾が重くて測れないなら物差しを替える**
　**三連単のROIは99%CI±31ptで「有意化に900年」→ 的中率に替えたら今のデータで判定できた**。
　**ROI = 的中率 × 1回あたり配当**。**オッズを揃えた対照なら配当が揃い、残るのは的中率の差だけ**。
　★**対数配当（幾何平均）でも効く**——**算術1.200倍 → 幾何1.057倍 → 中央値1.000倍**と分解できた。

★**45 内部対照は「乱を1粒も使わない量」に置く**
　**4回落とした。原因は (a)モンテカルロで揺れる量に揺れ幅より狭い許容を置いた
　(b)母集団の違う数字に厳しい許容を置いた のどちらかだけ**。
　⚠**判定基準32を守るほど高くつく**——**装置は正常なのに読むのをやめることになる**。
　★**ゲートの許容は測ってから決める**。

★**46 選択バイアスは「前半で選び、後半で1回だけ試す」で実測できる**
　**前半(2016-2020)だけで最良を選び、後半(2021-)で1回だけ試す**。
　★**プラセボにも同じ手続きを踏めば「選ぶだけでどれだけ縮むか」の物差しになる**。
　**実測では 本25.8pt > プラセボ12.3pt ＝ 選択バイアスで説明できる側**だった。

⚠**加えて古いほうから3つ**:
　**25 経路が違う結論を混ぜるな** / **32 ゲートが落ちたら先を読むな** /
　**42 主判定の統計量が「仮説が偽なら0を返す」と1行書けないなら使うな**。

---

## 4. データと道具（**すぐ使える**）

```
学習/検証の作り方  … ml/audit_gap_axis.py のmain冒頭をそのままコピーでよい
  F.to_model(F.load_files()) → build_features → keep=(n_prior>=1 & odds>0)
  cut = d["date"].quantile(0.3) で前30%学習・後70%検証
  fit_seeds(fx[tr], y[tr], 3, PAR)  ※PAR は CAPACITY["l2"]
差の作り方        … share = p/Σp（モデル） / mk = (1/odds)/Σ(1/odds)（市場） / gap = share − mk
払戻              … payoff(r, "単勝", [馬番]) / payoff(r, "複勝", [馬番]) / payoff(r, "ワイド", [a,b])
板                … load_board(type, keylen)  1単勝 2複勝 3枠連 4馬連 5ワイド 6馬単 7三連複 8三連単
                    ⚠ワイド(5)だけ未収集。枠連は load_boards()（キーがタプル・他は文字列）
CI                … zq(α) で z を出す。**Bonferroni は α を比較数で割る**
乱の実装の手本    … ブランチ claude/determined-allen-2s07uf の ml/audit_ana_seed.py（20種で揺れを実測）
```

★**回す前に必ず**: `python3 ml/check_sigs.py`（引数不一致の静的検出）。
　⚠**「データ待ち」で寝かせたスクリプトは腐る**（(124)の前例）。**自己テストを付ける**（(161)の `--selftest` が手本）。
⚠**netkeiba への取得はクラウドからは出られない（403）**。**`nk_fetch.py` / `nk_odds_bulk.py` は Mac 専用**。
⚠**`ml/model_prod/` は .gitignore なので空**。最初に **`python3 ml/train_prod.py`（約2分）**を1回。

---

## 5. ★★最後に（**これを忘れると方向を見失う**）

**このプロジェクトで100%を超える可能性が測定として残っているのは、枠連の比の裾((141) 112.5〜117.3%)だけ**。
⚠**それも確定オッズのオラクル**で、**張れる時点で成立するかは(148)＝時系列オッズ25開催日（いま8）が決める**。
　★**(148)は元セッションの担当**。**このセッションでは触らない**。
⚠**運用の長期期待値は 83〜91%（点推定86.7%）**。**上端でも100%に届かない**。
★**穴馬の線は「妙味は実在するが必要量の80〜82%で頭打ち」で終わった**。
　→ ★**候補A〜Cのどれが当たっても、それは道が1本増えるという意味**。**枠連の話とは混ぜない**。
