# ANA_MERGE_HANDOFF.md —— ★穴馬セッションから統合セッションへ

★**作成 2026-09-13**。★**穴馬側（ブランチ `claude/determined-allen-2s07uf`）が持っているものの全部**。
⚠**これは「決めたこと」と「まだ決まっていないこと」の引き継ぎであって、測定結果そのものではない**。
★**測定は `ANA_TRACK.md`（時系列）と `ROI_MAP.md`（地図）にある**。

---

## 0. ★★★最初に読む: **この線の正体**

★**「市場と推奨のズレがある穴馬のオッズ妙味」**。11年の全マス探索を経て、前向き運用を始めた。

| ⚠ | ★**先に知っておくべきこと** |
|---|---|
| **1** | ★★**11年の凍結値（113.4% / 128.8%）は、今の前向き運用に引き継がれない**（理由4つ・§6） |
| **2** | ★★**主判定は約500年かかり、決着しない**。★**それを承知で据えている**（後から基準を緩めないため） |
| **3** | ⚠**モデルは市場を超えたことがない**（(236)・AUC差は11年ゼロ近傍） |
| **4** | ⚠**該当レースは年354本→36本に減り続けている**（(234)(235)） |
| **5** | ★**前向き標本は 2026-09-12 開始・現在 1本**（9/12 中山1R・**外れ**） |

★**つまりこれは「儲かる線」ではなく「測れる形にした線」**。⚠**そう扱うこと**。

---

## 1. ★★★★動かしてはいけないもの（★凍結）

### ★軸（(210)で固定・以後一度も変えていない）
```
pn  = モデルの複勝確率をレース内で合計3に正規化
qp  = 複勝の板[下限,上限]の ★調和平均 を合計3に正規化
gap = pn − qp

軸 = pn ≥ 0.15 かつ gap ≥ 0.15 かつ 単勝オッズ ≥ 10.0倍 の中で ★pn 最大の1頭
```
★**性格**: **平均21.2倍 / 中央15.9倍 / 6.6番人気 / 複勝的中23.4%**

### ★買い目（2026-09-08 再凍結）
| | 買い目 | 紐 |
|---|---|---|
| ★**P馬単M4点**（★**主判定**） | `①-②/②-①/①-③/③-①` | **P** = モデル `pv` 降順 |
| ★**X三連単A4点** | `①-②-③/①-③-②/①-②-④/①-④-②` | **X** = モデル上位2頭＋人気順 |
★**8点・800円/レース**。⚠**変えたら前向き標本は切れる**（§5-2 規則4）。

### ★記録だけ（買わない）
**P複勝1点 / P単勝1点 / G馬単M4点 / Q三連単A4点** → ★**現在 G と Q が出せていない**（下記 §4-Q3）

---

## 2. ★★運用（2026-09-12 開始・`RACEDAY.md` が正）

| いつ | どこ | コマンド |
|---|---|---|
| **朝（9時台）** | ★**Mac** | `sh raceday_fetch.sh <日付>` |
| **朝（続けて）** | ★**クラウド** | `python3 ml/raceday.py <日付>` |
| **夜** | ★**Mac** | `sh raceday_results.sh <日付>` |
| **夜（続けて）** | ★**クラウド** | `python3 ml/raceday_night.py <日付>` |

⚠★★**Macには pandas / lightgbm が無い**。★**取得はMac・予想はクラウド**。これは変えられない。

■ ★**主要ファイル**
| | |
|---|---|
| `ml/nk_place_morn.py` | ★**朝9時の複勝板を取る**（★**Mac・標準ライブラリのみ**）。⚠**過去には遡れない** |
| `ml/reco_ana_day.py` | ★**当日の買い目を出す**（⚠**判定ロジックの本体。触るなら慎重に**） |
| `ml/train_ana_fwd.py` → `ml/model_ana_fwd/` | ★**固定モデル**（**year<2026 の562,075行・L2・3シード**） |
| `ml/raceday.py` / `ml/raceday_night.py` | ★**朝＝凍結／夜＝採点**（★**`reco_ana_day.py` を書き換えずフックで控える**） |
| `data/reco/ana_forward.csv` | ★★**前向き標本の本体**。`_pay = _ret ÷ (_cost/100)` |
| `data/raceday/<日付>/tickets.json` | ★**朝に凍結した買い目＋`git_head`**。★**夜の採点はこれだけを読む** |
| `data/nk_odds_morn/place<日付>.jsonl` | ★**朝9時の複勝板**。⚠**取らなかった日は永久に失われる** |

---

## 3. ★★★主判定（`ANA_RULE.md` §5-2・2026-09-12 登録済み・★変えない）

> ★**2026-09-12 以降に `reco_ana_day.py` が出した買い目だけで、
> `P馬単M4点` のROIの99%CI下端が 100% を超えるか。**

⚠**約500年。決着しない**。★**ゲート2（「線が死んだか」）だけは1年で読める形にしてある**:
| # | 量 | 凍結値 | ⚠**死んだと読む線** |
|---|---|---|---|
| ★① | **該当/年** | 年47.6本（朝9時なら約32本） | ⚠**年10本未満** |
| ★② | **軸の複勝的中率** | **23.4%** | ⚠**100本時点で15%未満** |
| ③ | 軸の平均単勝 | 21.2倍・6.6番人気 | ⚠**大きくずれる** |

★**読むのは年1回。読む前に何を読むか書く**（判定基準42）。

---

## 4. ⚠★★★**まだ決まっていないこと（★統合セッションが引き継ぐもの）**

### ★Q1. **板の取得時刻の揺れ**（⚠**一番重い・急ぎ**）
★**実測**: 9/12 **09:54** / 9/13 **10:23**。★§3 は「朝9時」で凍結。
⚠**(219)は「遅いほど確定に近い＝数字が良く出る方向」と測っている**。★**無制限のドリフトは標本を静かに甘くする**。
⚠**さらに `pn`（単勝 09:30〜10:20台）と `qp`（板）が別時点から来ている**。
★**穴馬側の提案（未採否）**:
　**(a) `ana_forward.csv` に `board_at` / `odds_at` 列を足す** ← ★**無条件でやるべき。今は復元不能**
　**(b) 上限を決める**（例: **板が10:30を過ぎた開催日は標本に入れない・記録は残す**）
　⚠**年32本では、あとから時刻で層別して読むことは不可能**。★**入口で切るしかない**。

### ★Q2. **障害戦を除外するか**（SNS設計セッションの発見）
★**生データのトラック種別は 芝/ダ の2値で「障」が無い**（`features.py:39`）
→ ★★**モデルは障害を芝として学習・予測している**。
★**実測（こちらの1,398本の母集団）**: **障害は軸が約2倍立つ（4.6→8.5%）のに複勝的中は 24.0→14.3%**。
→ ★**ズレが妙味ではなくモデルの誤差**。⚠**ROIの差は読まない**（n=77で符号が逆転する）。
★★**識別は完全にできる**: ★**`entries` の `surface == "障"`**（⚠向こうの距離推定より正確・実物で確認済み）。
★**穴馬側の推奨**: ★**除外**（機構・的中率・失うのは該当の5.5%・★標本1本の今が最も安い）。
⚠**9/13の中山1R（障害）はズレ+0.177 だったが単勝7.9倍で落ちただけ。次は通り得る**。

### ★Q3. **当日モードに G紐・Q紐 を足すか**
⚠**`reco_ana_day.py` は P と X しか作らないので、G馬単M4点 と Q三連単A4点 が記録できていない**。
★**`gap` と `od` から3行で作れる**。⚠**買い目も軸も変わらないので §5-2 規則4には当たらない**。
★**穴馬側の推奨**: ★**足す**（4本に減らすと §4-2 の「6本」を静かに狭めることになる）。

### ★Q4. **やめる基準**（⚠**2026-09-10 から未設定のまま**）
★**決めるなら記録を読む前に**。★**決めなければ「ゲート2の①②③が割れるまで続ける」が既定**。

---

## 5. ⚠★★**穴馬側が実際に踏んだ誤り（★同じ穴に落ちないために）**

| # | ⚠**誤り** | ★**教訓** |
|---|---|---|
| **1** | ★★**判定基準37を9回踏んだ**（**1,398 / 1,383 / 1,356 の取り違え**） | ★**新しいスクリプトの「最初の内部対照」を必ず「対象レース数」にする**。★**凍結値を引用するときは母集団を必ず併記** |
| **2** | ★★**「朝9時に `reco_ana.py` で推奨を出す」を、動かないまま運用手順に書いた** | ⚠**`reco_ana.py` は着順>0 の馬しか読まない＝未走では動かない**。★**手順は書く前に走らせる** |
| **3** | ★★**§5-2 で「判定する」と宣言しながら、保存する仕組みを作っていなかった** | ★**宣言と実装を同じコミットで揃える** |
| **4** | ★**(240)で「④はむしろ広がった」と書いたが se を出していなかった** | ★**差を語る前に se を出す**（(241)で訂正・実際は動いていない） |
| **5** | ★**(236)で「一度も超えていない」と書いたが正の年が3つあった** | ★**範囲と断定を同じ文に並べない**（枠連側の独立検算で発覚） |
| **6** | ★**`pgrep -f <名前>` が自分の親シェルを誤検知** | ★**自分と祖先の pid を除く** |
| **7** | ★**選択肢Aの説明で「凍結値がそのまま使える」と書いた** | ⚠**§3に「11年の数字は朝9時版に当てはまらない」と自分で書いてあったのに矛盾させた**（判定基準33） |

★**うち3件（5・7・Q2）は他セッションの独立検算で見つかった**。★**相互検算は効いている**。

---

## 6. ★★**守ってきた境界**（⚠**統合後も守るべきか、統合セッションが決める**）

| | |
|---|---|
| ★**枠連の運用**（紐1・除外40%・枠連） | ⚠**穴馬側は一切触っていない。設定変更の提案もしていない** |
| ★**`data/odds_ts`（時系列オッズ・(148)）** | ⚠**読むだけ。収集には触っていない** |
| ★**`ml/predict_nk.py` / `ml/model_prod/`** | ⚠**読むだけ。1行も書き換えていない** |
| ★**`ml/reco_ana.py`** | ⚠**SNS側の `--sns` が入っているので触っていない** |

⚠★**採番が衝突している**: **複数セッションが (233)〜(243) を別々の測定に使っていた**。
★**番号ではなくスクリプト名で特定すること**:
　(238)=`audit_ana_alloc.py` / (240)=`audit_ana_longcal.py` / (241)=`audit_ana_mix.py` /
　(243)=`train_ana_fwd.py` / (244)=`reco_ana_day.py`

---

## 7. ★**測定として閉じているもの**（★やり残しの測定は無い）

| | ★**結論** |
|---|---|
| **(216)** | ★**紐にモデルは不要、むしろ有害**（Q > P） |
| **(226)** | ★**G馬単Mは裾で出来ていた**（0.821）→ 閉じた |
| **(234)(235)** | ★**穴馬の供給は不変（0.93倍）。減っているのはズレだけ（0.33倍）** |
| **(236)** | ★**モデルは賢くなったが市場を超えたことがない**。★**「汚染前に戻す」先が無い** |
| **(238)** | ★**資金配分の最適化は等分に5.5倍負ける**＝**配分は判定に使わない**（利用者決定） |
| **(240)(241)** | ★**市場は穴の「並べ方」だけ上手くなった。「水準」は11年ずれたまま**（−0.006〜−0.009・過大評価） |

★**未測定で残っているのは「オッズを特徴量から抜いたモデル」だけ**（⚠**別の線を開ける話・利用者の判断待ち**）。

---

## 8. ★**利用者との約束（★守ること）**

| | |
|---|---|
| **1** | ★**勝手に判断しない**。★**99%CI下端の結果は利用者が読む**。私は材料を出す |
| **2** | ★**事前登録してからコミットし、それから走らせる**。★**結果を見てから基準を変えない** |
| **3** | ★**軸は穴馬のまま**。⚠**的中率を上げるために人気馬に寄せない** |
| **4** | ★**枠連の運用には手を出さない。設定変更の提案もしない** |
