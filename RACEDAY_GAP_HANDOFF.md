# ⚠★★★★運用ハブから3セッションへ —— **結果CSVに1日穴があり、モデルが勝ち馬を表から落としていた**（**2026-09-19**）

★**ブランチ** `claude/dreamy-keller-2p8bmf`（**`f519f02` で push 済み**）。
★**読み方**: `git fetch origin && git merge origin/claude/dreamy-keller-2p8bmf`。
⚠**判定ロジック（`predict_nk.py` / `reco_ana_day.py` / `reco_sns_day.py` の中身）は1行も書き換えていない**。
★**足したのは点検と記録だけ**。

---

## 1. ★★何が起きたか

★**2026-09-19 中山3R**（芝1600m・２歳未勝利・12頭）で、
⚠**単勝1.5倍・1番人気のトルタパスカリーナが、両モデルのスコア表に1行も載らないまま1着**になった。

| | |
|---|---|
| ★**朝の出力** | `※過去走なし2頭: トルタパスカリーナ/イニシャルポイント（来たら外れる）` |
| ★**表の頭数** | ⚠**12頭立てなのに10頭**（本命モデルも穴馬モデルも） |
| ⚠**結果** | ★**5-1-2**。**落とした5番が1着** |

★**両モデルとも2・3着は当てている**（穴馬の軸1番＝2着、紐2の2番＝3着）。⚠**1着だけが表に存在しなかった**。

■ ⚠★★**原因は判定ではなくデータの穴**
　★**彼女の唯一の前走が 2026-07-25**。⚠**その日の `data/nk/DSnk20260725.CSV` が手元に無い**。
　→ ★**過去走が繋がらない馬は特徴量が作れないので、モデルが表から落とす**。
　★**出馬表 `DG260725.CSV` には名前が残っている**ので、⚠**走ったことは分かるが着順が無い**。

■ ★**穴は1日だけ**
　★**手元の期間 20260719〜20260919 で欠けているのは `20260725`（土）のみ**。
　⚠**7/18 は収集開始より前なので穴ではない**（★ハブが最初「7/18・7/25」と言ったのは誤り・訂正済み）。

■ ⚠★★**影響は9/19だけではない**
　★**7/25 にデビューした2歳馬は、埋めるまで今後もずっと「過去走なし」**。
　⚠**3セッションとも同じ `data/nk/` を読んでいる**ので、★**共通の土台の問題**。

---

## 2. ★やったこと（`f519f02`）

| | ★**中身** |
|---|---|
| ★**`ml/nk_gaps.py`**（新） | ★**手元の期間内で土日なのにファイルが無い日を探す**。★穴があれば終了コード1＋埋めるコマンド |
| ★**`ml/raceday.py`** | ★**朝の一括が「■ 過去走データ」として毎回表示**。⚠**止めはしない**（★取得はMac側なのでその朝には間に合わない） |
| ★**`tickets.json`** | ★**`results_gaps` を記録**——⚠**その日の推奨を作った時点で欠けていた日**が凍結記録に残る |
| ★**`raceday_results.sh`** | ★**夜にも点検**。★**netkeiba に届く唯一の場所なので、その場で埋められる** |
| ★**`RACEDAY.md` §3-3** | ★経緯と手順 |

★**`nk_gaps.py` はカレントディレクトリに依存しない**（★リポジトリ直下・`ml/`・`/` の3か所から叩いて同じ答えを確認済み）。

```
python3 ml/nk_gaps.py
⚠★★**結果CSVに穴が 1日ある**（20260719〜20260919）
　　20260725(土)
```

■ ⚠★**見つけ方の限界**（★先に書いておく）
　★**土日しか見ていない**。⚠**振替開催などの平日開催は取りこぼす**。
　★**偽の警告を出すよりまし**と判断した。⚠**測定ではなく、私の判断**。
　★**期間の外（収集開始より前）は穴として数えない**。

---

## 3. ⚠★★埋めるのは Mac 側（**順番厳守**）

⚠**クラウドからは netkeiba に届かない**（★実測 `HTTP 000`）。

```
python3 ml/nk_fetch.py results 20260725
python3 ml/nk_fetch.py pedigree
python3 ml/nk_link.py
```
⚠★**`pedigree` を飛ばすと血統が永久に消える**（★**2026-09-12 に一度やらかしている**・`RACEDAY.md` §3-2）。

---

## 4. ★★各セッションに見てほしいこと（★**3つとも共通**）

| # | ★**問い** | ★**ハブの立場** |
|---|---|---|
| ★**1** | ★**過去の測定に効くか**。⚠**7/25組の2歳馬が母集団に入っている期間の数字は、その馬が落ちた状態で出ている** | ★**各セッションの領分**。★**凍結値や標本の前提が動くなら教えてほしい** |
| ★**2** | ★**埋めたあとに過去を再計算するか** | ⚠**9/19 の判定は作り直すべきでない**（★朝の時点で持っていなかったデータなので**後知恵**になる）。★**監査スクリプトの数字は別で、そちらは各セッションの判断** |
| ⚠★**3** | ★**「過去走なし」で黙って落とす扱いそのもの**を変えるか | ⚠★**判定ロジックなのでハブからは触らない**。★現状は**落として `pn` の Σ=3 を残りの頭数で取り直す**（`ANA_RULE.md` §6 の相違4） |

⚠★**3を動かすなら、記録を読む前に**（判定基準42）。

---

## 5. ★ついでに: 2026-09-19 の夜の採点は済んでいる（`f038f47`）

| | 買い目 | 結果 |
|---|---|---|
| ★**枠連** | 中山8R 4-4 100円 | ★**0円**。⚠**軸6番マタミカンサンは1着、紐枠が外れ**（2着は1番＝1枠） |
| ★**甘い軸** | 阪神8R 2-5-6 100円 | ★**0円**。着順 **6-1-2**、5番が4着 |
| ★**穴馬** | ★**該当0本** | ★**前向き標本は1本のまま**（9/12 中山1R） |
| ★**SNS** | 中山10R ◎10 / 中山12R ◎3 | **8着・8着** |

★**正典（`ml/nk_score.py`）との照合＝一致**。
⚠★**9/19 は取得が 11:05 だったため、発走済み6レース（中山1-3R・阪神1-3R）を落としている**
　（`DROP_STARTED`・`RACEDAY.md`）。★**中山3Rもそこに入るので、上の穴は買い目には影響していない**。
