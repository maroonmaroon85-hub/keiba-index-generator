"""(249sns) ★★★**単勝8.0倍 × ズレ0.08 —— 2つ同時に緩めるとどうなるか**（利用者の指定・2026-09-15）

★**利用者の指定**: **「単勝の下限8.0 ズレ0.08で」**。
⚠★★**これは2つ同時に緩める**——**(246sns)は単勝だけ、(240sns)(247sns)はズレだけを動かした**。
　★**両方動かした場合は測っていない**。**掛け合わせは足し算にならない**ので測る。

■ ⚠★★★**先に線を引く（★これが一番大事）**
　★**`ANA_SNS_BRIEF.md` の禁止事項4**: **「的中率を上げるために軸を人気馬に変えること」**。
　★**単勝の下限は◎が穴馬であることの根拠そのもの**。
　★★**(246sns)で分かっていること**: **単勝を下げると★崖は無く滑らかに人気側へ寄る**
　　（**平均人気 10.0倍:6.7 → 8.0倍:6.2**）。★**中央人気は7.0倍まで6番のまま**。
　⚠★**だから「8.0倍はまだ穴馬か」は★平均人気と中央オッズで見る**。**中央人気だけでは鈍い**。
　★★**これは測るだけ。採用の提案ではない**。★**採るかどうかは利用者が決める**。

■ ★測るもの（**3つを並べる**）
| | 単勝 | ズレ |
|---|---|---|
| ★**現行** | **10.0** | **0.10** |
| ★**指定** | ★**8.0** | ★**0.08** |
| 参考（片方だけ） | **8.0** | **0.10** |
　★**障害戦は除外**（(243sns)）。**他の条件は一切動かさない**。
　★**出す量**: **該当数 / 1日 / ★中央人気 / ★平均人気 / ★中央オッズ / 複勝的中 / ★複勝ROI / 単勝的中**
　　＋★**日別**（**該当0本の開催日 / 複勝が当たる日 / 連続で当たらない最大**）。

■ ★★★★**ゲート2（判定基準42）—— ★何を返せば「採ってはいけない」か**
　★**① 平均人気が 5番台に入る**なら、★**◎は人気側の馬になった**＝**禁止事項4に触れる**。
　★**② 中央オッズが 13倍を切る**なら、**「◎はいつも人気薄」が苦しくなる**。
　★**③ 1日の本数が 10本を超える**なら、⚠**1日24レース中4割に印**＝**「絞っている」と言えない**。
　⚠★**「的中率が上がる」は情報を持たない**——**両方緩めるのだから★機械的に上がる**。

■ ★★内部対照（⚠**落ちたら読まない**）
　★**10.0 / 0.10 が 5,015本・複勝的中22.0%・中央オッズ16.2倍**（**(243sns)平地**）。
　★**8.0 / 0.10 が 6,139本・24.8%・14.1倍**（**(246sns)**）。

■ 予想（⚠**当てにしない**・★このセッションは7勝6敗）
　★**該当は1日10本前後**と見る（**8.0/0.10で5.37本、10.0/0.08で7.09本なので、掛け合わせで10本弱**）。
　★**平均人気は5.8〜6.0番**と見る（**8.0/0.10が6.2番。ズレを緩めても人気は動きにくいはず**）。
　★**中央オッズは13.5〜14.5倍**、★**複勝的中は24〜26%**、★**ROIは92〜94%**と見る。
　⚠**もし平均人気が5番台前半まで落ちるなら、★ズレと単勝は独立ではない**——
　　**そのときは「片方ずつ測った結果は足し算にならない」という一般的な教訓になる**。


■ ★★★★**結果（実測済み・2026-09-15）**　★**内部対照2つとも通過**

| 単勝 | ズレ | 該当 | ★**1日** | 中央人気 | ★**平均人気** | ★**中央オッズ** | 複勝的中 | ★**複勝ROI** |
|---|---|---|---|---|---|---|---|---|
| **10.0** | **0.10** | 5,015 | **4.39** | 6番 | **6.7** | **16.2倍** | 22.0% | **93.9%** |
| ★★**8.0** | ★★**0.08** | **9,657** | ★**8.46** | **6番** | **6.2** | **14.3倍** | 24.4% | ⚠**92.2%** |
| **8.0** | **0.10** | 6,139 | **5.38** | 6番 | **6.2** | **14.1倍** | 24.8% | ★**94.5%** |

| 単勝 | ズレ | ★**該当0本の日** | ★**複勝が当たる日** | ★**連続外れ最大** |
|---|---|---|---|---|
| 10.0 | 0.10 | 4.5% | 58.0% | 10日 |
| ★★**8.0** | ★★**0.08** | ★★**0.2%** | ★★**85.6%** | ★★**4日** |
| **8.0** | **0.10** | 2.3% | 69.5% | 6日 |

■ ★★★★**ゲート2の答え: ★3つとも踏んでいない**
　**① 平均人気 6.2（境6.0）／② 中央オッズ 14.3（境13.0）／③ 1日8.46本（境10.0）**

■ ★★★★**いちばん効いた発見: ★掛け合わせは足し算にならない（★2つは独立に働く）**
　★**平均人気は 8.0/0.10 も 8.0/0.08 も★同じ6.2番**。**中央オッズは 14.1→14.3倍で★むしろ上がる**。
　★★**つまり◎を人気側へ動かしているのは★単勝の下限だけ**。⚠**ズレの下限は人気に効いていない**。
　★**ズレは★本数だけを増やす**——**(240sns)の「ズレを下げても◎の見た目は変わらない」と整合**。
　★★**単勝＝◎の性格 / ズレ＝本数**。★**役割がきれいに分かれている**。

■ ⚠★★**ROIは掛け合わせで落ちる（★落としているのはズレの方）**
| | ROI |
|---|---|
| **10.0 / 0.10**（現行） | **93.9%** |
| ★**8.0 / 0.10** | ★**94.5%**（**現行より★高い**） |
| ⚠**8.0 / 0.08**（指定） | ⚠**92.2%**（**3構成で最低**） |
　★**単勝を8.0に下げるだけなら ROIは上がる**。⚠**落としているのは★ズレの0.08**。

■ ★予想の答え合わせ（判定基準24・★このセッション9回目）
| 予想 | 実際 | |
|---|---|---|
| **1日10本前後** | ★**8.46本** | ★**当たり**（やや少なめ） |
| **平均人気 5.8〜6.0番** | ⚠**6.2番**（**8.0/0.10と同じ**） | ⚠**外れ**（**ズレは人気に効かなかった**） |
| **中央オッズ 13.5〜14.5倍** | ★**14.3倍** | ★**当たり** |
| **複勝的中 24〜26%** | ★**24.4%** | ★**当たり** |
| **ROI 92〜94%** | ★**92.2%** | ★**当たり** |
　★**外した1つが★いちばん重要だった**——**「ズレと単勝は独立」は、
　　★片方ずつ測っていては分からなかった**。⚠**掛け合わせを測って初めて出た**。

■ ★**読み方（⚠採用の提案ではない。★決めるのは利用者）**
　★**8.0/0.08 はゲートを1つも踏んでいない**。**1日8.5本・該当0本0.2%・当たる日85.6%・
　　連続外れ最大4日**で、★**SNSとしては圧倒的に回しやすい**。
　⚠**引っかかるのは2点**:
　　**① 1日24レース中8.5本＝★35%に印**——**ゲート③は踏まないが「絞っている」看板は薄まる**
　　**② ROI 92.2% は3構成で最低**——⚠**買わないので直接の損は無いが、
　　　★「買っても損しない線」とは言えなくなる**
　★★**8.0/0.10 が、現行より★全項目で良い**（**ROI 94.5% / 1日5.38本 / 当たる日69.5% /
　　連続外れ6日**）。⚠**ズレを0.08にするぶんが★ROIだけを削っている**。

実行: python3 ml/audit_ana_l8g08.py
"""
import os
import sys

import numpy as np

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

import features as F
from audit_crosspool import load_races, payoff
from audit_ana_odds import MIN_HORSES, roi_of
from audit_ana_marg import wf_predict
from audit_ana_board import NPLACE, load_fuku_boards, qpool
from audit_ana_band import PN_FLOOR
from train_prod import add_odds_features

COMBOS = [(10.0, 0.10, "★現行"), (8.0, 0.08, "★★指定"), (8.0, 0.10, "参考")]
KNOWN = {(10.0, 0.10): (5015, 22.0, 16.2), (8.0, 0.10): (6139, 24.8, 14.1)}


def is_jump(dist):
    try:
        v = float(dist)
    except (TypeError, ValueError):
        return False
    return v > 0 and (v % 100 != 0 or v > 3600)


def runs_of_misses(hits):
    idx = [i for i, h in enumerate(hits) if h]
    return [b - a - 1 for a, b in zip(idx, idx[1:])] if len(idx) >= 2 else []


def main():
    print("(249sns) ★★★**単勝8.0倍 × ズレ0.08 —— 2つ同時に緩める**（利用者の指定）")
    print("⚠★**これは測るだけ。採用の提案ではない**"
          "——**単勝の下限は◎が穴馬であることの根拠**\n")

    races = {r["rid"]: r for r in load_races()}
    boards = load_fuku_boards()
    d = F.to_model(F.load_files())
    f = F.build_features(d)
    keep = (f["n_prior"] >= 1) & d["odds"].notna() & (d["odds"] > 0)
    d, f = d[keep].reset_index(drop=True), f[keep].reset_index(drop=True)
    y = (d["finish"] <= 3).astype(int).to_numpy()
    fx, _ = F.encode_categoricals(f)
    fx = add_odds_features(fx, d["odds"].to_numpy(float), d["raceid"].to_numpy())
    pred = wf_predict(d, fx, y, 3)
    m = ~np.isnan(pred)
    sub = d.loc[m, ["raceid", "umaban", "odds", "date", "distance"]].copy()
    sub["p"] = pred[m]

    K = {(L, G): {"od": [], "rk": [], "fu": [], "ta": []} for L, G, _ in COMBOS}
    DAY = {(L, G): {} for L, G, _ in COMBOS}
    yrs, days = set(), set()
    for rid, g0 in sub.groupby("raceid"):
        rid = str(rid)
        r, bd = races.get(rid), boards.get(rid)
        if r is None or bd is None or is_jump(g0["distance"].iloc[0]):
            continue
        nums = {u for u, _, _ in r["horses"]}
        if len(nums) < MIN_HORSES:
            continue
        gg = g0[g0["umaban"].astype(int).isin(nums)]
        ub = gg["umaban"].astype(int).to_numpy()
        if len(gg) < MIN_HORSES or not all(int(u) in bd for u in ub):
            continue
        od = gg["odds"].to_numpy(float)
        pv = gg["p"].to_numpy(float)
        if not np.isfinite(od).all() or (od <= 0).any() or pv.sum() <= 0:
            continue
        pn = pv / pv.sum() * NPLACE
        qp, _R = qpool([bd[int(u)] for u in ub], "harm")
        gap = pn - qp
        rank = np.argsort(np.argsort(od)) + 1
        dt = gg["date"].iloc[0].date()
        yrs.add(dt.year); days.add(dt)
        for L, G, _ in COMBOS:
            rec = DAY[(L, G)].setdefault(dt, [0, 0])
            c = np.where((pn >= PN_FLOOR) & (gap >= G) & (od >= L))[0]
            if not len(c):
                continue
            i = int(c[int(np.argmax(pn[c]))])
            ax = int(ub[i])
            v0 = payoff(r, "複勝", [ax])
            if v0 is None:
                continue
            vt = payoff(r, "単勝", [ax])
            a = K[(L, G)]
            a["od"].append(float(od[i])); a["rk"].append(int(rank[i]))
            a["fu"].append(float(v0)); a["ta"].append(float(vt) if vt is not None else 0.0)
            rec[0] += 1
            rec[1] += 1 if v0 > 0 else 0

    ny, nd = max(len(yrs), 1), max(len(days), 1)
    print("■ ★★**内部対照**")
    ok = True
    for key, (wn, wh, wo) in KNOWN.items():
        a = K[key]
        n, h, o = len(a["fu"]), 100.0 * np.mean(np.array(a["fu"]) > 0), float(np.median(a["od"]))
        g = abs(n - wn) <= 5 and abs(h - wh) <= 0.5 and abs(o - wo) <= 0.5
        ok &= g
        print(f"　単勝{key[0]}/ズレ{key[1]}　{n:>6,}本 {h:>5.1f}% {o:>5.1f}倍"
              f" vs {wn:>6,} {wh:>5.1f}% {wo:>5.1f}倍　{'★通った' if g else '⚠落ちた'}")
    if not ok:
        print("\n⚠⚠**内部対照が落ちた。読まない**（判定基準32）。")
        return

    print(f"\n■ ★★★**軸の性格と水準**（**11年 / {ny}年 / {nd:,}開催日・障害除外**）")
    print(f"{'単勝':<6}{'ズレ':<6}{'該当':>8}{'1日':>7}"
          f"{'★中央人気':>11}{'★平均人気':>11}{'★中央オッズ':>12}"
          f"{'複勝的中':>10}{'★複勝ROI':>11}{'単勝的中':>9}")
    for L, G, tag in COMBOS:
        a = K[(L, G)]
        fu, ta = np.array(a["fu"]), np.array(a["ta"])
        rk, odd = np.array(a["rk"]), np.array(a["od"])
        print(f"{L:<6.1f}{G:<6.2f}{len(fu):>8,}{len(fu)/nd:>7.2f}"
              f"{np.median(rk):>10.0f}番{rk.mean():>11.1f}{np.median(odd):>11.1f}倍"
              f"{100*np.mean(fu>0):>9.1f}%{roi_of(fu):>10.1f}%{100*np.mean(ta>0):>8.1f}%  {tag}")

    print(f"\n■ ★★**日別**（**{nd:,}開催日**）")
    print(f"{'単勝':<6}{'ズレ':<6}{'★該当0本の日':>14}{'★複勝が当たる日':>16}"
          f"{'★連続外れ最大':>14}")
    for L, G, tag in COMBOS:
        dd = DAY[(L, G)]
        nz = sum(1 for v in dd.values() if v[0] == 0)
        hit = [v[1] > 0 for _, v in sorted(dd.items())]
        rr = runs_of_misses(hit)
        print(f"{L:<6.1f}{G:<6.2f}{100*nz/len(dd):>13.1f}%"
              f"{100*sum(hit)/len(dd):>15.1f}%{(max(rr) if rr else 0):>12}日  {tag}")

    print(f"\n■ ★★★★**ゲート2の答え（★指定の 8.0 / 0.08 について）**")
    a = K[(8.0, 0.08)]
    rk, odd = np.array(a["rk"]), np.array(a["od"])
    fu = np.array(a["fu"])
    mr, mo, npd = rk.mean(), float(np.median(odd)), len(fu) / nd
    for lab, v, th, bad in (("① 平均人気", mr, 6.0, mr < 6.0),
                            ("② 中央オッズ", mo, 13.0, mo < 13.0),
                            ("③ 1日の本数", npd, 10.0, npd > 10.0)):
        print(f"　{lab} {v:.1f}（境 {th}）　→ "
              + ("⚠**踏んだ**" if bad else "★**踏んでいない**"))
    print("\n⚠★**「的中率が上がる」は情報を持たない**（**両方緩めるので機械的に上がる**）。")


if __name__ == "__main__":
    main()
