# 引き継ぎメモ（新しいセッションの最初に貼る用）

`HANDOFF.md` が本体（3,700行）。**これは「まず何を読み、何をやり、何をやらないか」だけの案内**。

---

## 0. まずこれを言う（新セッションへの指示文）

> 競馬の予想モデルのプロジェクト。作業ディレクトリは `keiba-index-generator`。
> **まず `NEXT_SESSION.md` を読み、次に `HANDOFF.md` の「★判定基準」と「次にやること」を読んで**。
> 110回近い検証を積んでいるので、**同じ検証をやり直さないこと**。
> 「もう戻らないこと」に載っている方向には戻らない。数字を報告する前に必ず判定基準を当てること。
>
> **★目標は ROI 100% 超え。まだ諦めていない。**
> ただし「頑張って探す」ではなく、**残っている道を一本ずつ潰しながら、
> 潰れていない道に資源を集中する**やり方で進める。下の「6. 100%を目指すために次に試すこと」が
> その一覧。**閉じた道に戻るのが最大の時間の無駄**なので、そこだけは厳守すること。

---

## 1. 今どこにいるか

### 結論（変わっていない）
**実運用の最良は ROI 85%。単勝オッズだけを使う戦略の実測上の到達点は複勝96.8%。**
どちらも100%に届いていない。

### ★勝負の形は「D」という1つの数字に還元されている
```
D = E[log q] − E[log q_pool]      1レースあたり対数成長率 = log(払戻率) + D
★儲かる条件:  D ≥ |log(払戻率)|
   必要量: 複勝/単勝 0.2231 / 枠連・馬連 0.2549 / 三連複 0.2877 / 三連単 0.3216
```
**ケリーが上限なので、Dが足りなければ買い方をどう変えても儲からない**（(89)④）。
逆に言えば、**やるべきことは「q に何を入れるか」だけ**。買い方・点数・資金配分の探索は無意味。

### ★100%までの距離（枠連・2026-08-06時点）
| q の作り方 | D | 必要量(0.2549)に対して |
|---|---|---|
| Harville・市場のみ | +0.0142 | 5.6% |
| ＋λ補正（(96)で変換の系統誤差を修正） | +0.0182 | 7.1% |
| ＋モデルを混合（(100)(102)） | **+0.0207** | **8.1%** |
| **必要量** | **0.2549** | 100% |

**残り92%。** これが現在地。**「あと少し」ではないことを直視した上で、それでも道を探す**。

### ★現時点の最良の q（これをそのまま使えばよい）
```
1. 市場 p_mkt = 正規化した1/オッズ                        … 土台。単体でこれが最強
2. 対数線形プールで混合:
   q ∝ p_mkt^0.847 · p_L2win^0.060 · p_L2top3^0.067 · p_L5win^0.027
3. 2着・3着の段に λ を当てる（Harvilleの系統誤差の補正）
   市場側 λ2≈0.85 / λ3≈0.72 ／ 1着の段は τ≈1.0 で補正不要
```

---

## 2. 環境の再構築（クラウドは毎回まっさら）

```bash
pip install lightgbm scikit-learn pandas numpy joblib
python3 ml/train_prod.py          # ★実運用モデルを作る（約2分）
```
- `ml/model_prod/` は **.gitignore なので新環境では空**。作らないと `predict.py` が動かない。
- `train_prod.py` が **枠連84.5% / 三連複BOX4 84.5%** を表示すれば環境は正常。

### ⚠**長い計算はバックグラウンドで回さない**（2026-08-06に判明）
**セッションが idle になるとコンテナごと再起動され、バックグラウンドのプロセスは殺される**
（`uptime` が `up 0 min` になる。メモリは常に空いていたのでOOMではない）。
→ **`timeout 570 python3 ...` で前面実行する**。1ターンあたり9分ほど確実に回る。
→ **年ごとに結果をCSVへ保存する構造にしておく**（`data/cache/exp_*/{年}.csv`）。
　 落ちても続きから走るので、`ls data/cache/exp_*/ | wc -l` で進捗が見える。

### 実行場所の分担（再検討しないこと）
**netkeibaはクラウドから取得できない**（プロキシがCONNECTを403で拒否）。
| 作業 | 場所 | 依存 |
|---|---|---|
| 取得 `nk_fetch.py` / 名寄せ `nk_link.py` / 答え合わせ `nk_score.py` / 表 `reco_table.py` | **Mac** | 標準ライブラリのみ |
| **予想 `predict_nk.py` / 学習 `train_prod.py`** | **クラウド** | pandas/numpy/lightgbm |

★**Macにpandasは入っていない**。取得系に依存を足さないこと。

---

## 3. 守る作法（★これを飛ばすと必ず偽陽性が出る）

`HANDOFF.md` の「★判定基準」を読む。要点：

1. **指標・閾値・判定条件は測る前に宣言する**。最良のビンで判定しない。**単調性（Spearman ρ）で見る**。
2. **プラセボ対照を必ず並べる**。中身を空にした偽データで同じ手続きを踏み、**そこで効果が消えること**を確認する。
   （今回 (100)(102)(104)(107) はすべてこれで守られた。**これが無い主張は信じない**）
3. **交絡対照を並べる**。「その指標でなければ駄目か」を、似た別の指標で潰す（(95)で cv vs p_top）。
4. **わざと有利にして潰す**。上界を作って、**上界ですら届かないなら閉じる**（(101)(109)）。
5. **ウォークフォワード**（各年をそれ以前の年だけで学習）。パラメータを当年で当てはめない。
6. **買い方どうしの比較はROIでなく「1レース期待損失（円）」**。ROI差は検出限界未満。
7. **数字をHANDOFFに書くならスクリプトを同じコミットに入れる**（(92)で検算不能な数字を残した）。
8. **★集める前に必要標本数を計算する**（(108)）。これをやらずに集めたので(92)⑤(94)④は判定不能だった。
9. ⚠**Dが使えるのは「すべての結果が観測される」場合だけ**（(99)）。複勝は着内馬の払戻しか
   公表されないので、**馬の属性で層別したDは上方バイアスを持つ**。層別は実測ROIで見ること。
10. **シードは、Dで測るなら1本でよい**（(102)③で実測）。「1シードで報告しない」は**部分集合ROIの話**。

---

## 4. もう戻らないこと（すべて実測で否定済み・**ここに戻るのが最大の無駄**）

**モデル側（完全に閉じた）**
- 特徴量の追加（血統・馬場・母の半兄弟・レース内相対）— **オッズが全部吸収する**（6例以上）
- ハイパラ最適化 / 較正 / 層別（場・季節・頭数・馬場・血統・クラス）
- 残差学習・市場との差分学習（(75)。残差のみの順位付けは AUC 0.3605＝逆相関）
- **特徴を制限したモデルを混ぜる**（(107)。3種とも重み0.000）
  → ★**容量・正則化の違いは情報を足すが、情報集合の制限は足さない**
- **シード間不一致による除外**（(95)(104)。効くが差は0を超えず、混合比に変えても利得ゼロ）

**買い方側（上界で一括否定）**
- 買い方・点数・軸紐・資金配分の探索（(89)④）
- 指数での選別・絞り込み（(84)(85)(86)）
- **レースの選択**（(101)。完全オラクルは必要量を超えるが、**予測の相関が 0.000〜0.008**）

**市場側**
- 小倉が良い（(73)→(76)(91)で消滅）／ 季節・夏競馬（(91)）／ ローカル開催（(91)。向きが逆）
- 前日の馬場傾向（(74)）／ 実効控除率のばらつき（(103)。使える幅は±0.61pt）
- **オッズの時系列変化を信号にする**（(109)。**上界ですら利得ゼロ**）

---

## 5. 今日（2026-08-06）分かったこと — ★次の人が一番得をする部分

**測り方が間違っていた（2件）**
- **(96) Harville変換に系統誤差**。2着・3着の段を `p^λ` に一般化すると λ2≈0.85 / λ3≈0.72。
  「市場が下手」と思っていた分の**半分以上がこちらの計算ミス**だった（三連単 −0.0911 → −0.0386）。
- **(99) 層別した複勝のDは原理的に計算できない**（作法9）。

**新しく分かった（3件）**
- **(100) モデルは市場の上に情報を積んでいる**（プラセボ通過・初の直接証拠）。
  「モデルを捨てた方が強い」は**単体で使えば、に限定して訂正**。
- **(102) 混合は「1人ずつ足して効くか」では判断できない**。L2-top3 は2人構成では重み0.009＝無価値
  に見えたが、L5を入れた瞬間に0.047に跳ねた。**容量の違う相手がいて初めて目標の違いが活きる**。
- **(105)(106)「妙味の無い組は買われない」は市場全体の性質**。7券種すべてで
  「その組の期待払戻が小さいほどROIが線を上回る」が単調に出た。最良は**複勝96.8%**
  （2%裾・年間60R・99%CI[93.9,99.6]で**100%を除外**）。天井の機構も判明：
  **最低配当100円の床が払戻を押し上げるが、的中率が92%で頭打ち**なので `0.92×105円` が上限。

**予想を外した記録（正直に残す）**
- (95)「順位の不安定さが本命」→ 外れ。効くのは確率の揺れ。**モデル自身の自信で除外するのは逆効果**
- (99)「λ補正でDは下がる」→ 逆に上がった
- (105)「三連複は下位」→ 複勝と並ぶ規模で甘い ／ (106)「三連複は天井が無いから伸びる」→ 伸びない
- (107)「オッズを落としたモデルが最も効く」→ 重み0.000
- **★何度も出たパターン: 「効果の向きは正しく見えるのに、量が無い」**（(74)(104)(109)）。
  **向きだけ見て量を測らないと必ず騙される**。

---

## 6. ★100%を目指すために次に試すこと（諦めない）

**現状認識**: 情報を足す道（モデル・時系列オッズ・レース選択）は**ほぼ全部閉じた**。
残り0.23nat は、**これまでの積み上げ（1つ +0.002程度）の100倍**。
→ **同じ種類の努力を続けても届かない。カテゴリが違う道を探す必要がある**。

### A. すぐできる（クラウドだけ・データ追加不要）
1. **★全組み合わせから「期待払戻が最小の組」を選ぶ**
   (105)(106)は**人気上位N頭に固定**して測った。だが機構は「期待払戻が小さい組は買われない」
   なので、**全組み合わせを走査して最小の組を選べば、もっと甘い点が見つかる可能性がある**。
   λ補正Harvilleで全組の確率が計算できるので**発走前に選べる**。★これは未着手。
2. **床の外側で複勝96.8%を超える構成を探す**
   (106)の天井は「床が効く領域」の話。**期待払戻100〜130円の帯**（床が効き始める直前）を
   細かく刻めば、`的中率×払戻` の積が最大になる点が96.8%より上にある可能性がある。

### B. データが要る（Mac作業から）
3. **控除率そのものが低い商品を探す**
   ★**これが唯一の「カテゴリが違う道」**。情報で20%の控除率を埋めるのは無理だと分かったので、
   **控除率が実質的に下がる構造**を狙う。候補は**WIN5などのキャリーオーバーがある券種**
   （繰越金が乗ると実効払戻率が100%を超えうる）。手元の払戻データには**WIN5が無い**ので、
   まずデータの入手可否から。★**期待値はここが一番大きい**。
4. **枠連の発走前オッズ**（優先度最下位）
   (96)で「枠連プールの優位はHarvilleの誤差ではない」と確定したので**留保は既に外れている**。
   集めても(89)④の上界は動かない。**やる価値は低い**。

### C. やってはいけないこと
- **閉じた道に戻る**（4章の一覧）。特に「特徴を足す」「買い方を変える」「レースを絞る」。
- **プラセボ・交絡対照なしで主張する**。今日の成果はすべてこの2つで守られている。
- **向きだけ見て量を測らない**。

---

## 7. 現行の運用（変えていない）

| 目的 | 買い方 | 1R期待損失 | コスト | ROI |
|---|---|---|---|---|
| **本命** | **枠連 軸枠×紐枠1**（軸馬の枠 × モデル2位馬の枠・**1点**） | 14.8円 | 100円 | 85.2% |
| **対抗（測定が最も確か）** | **複勝 top1**（CIが3倍狭い） | 15.2円 | 100円 | 84.8% |
| 買うな | 三連複BOX4（+42.9円余計に失う）/ 三連単 / 9頭立て以下 / 券種の併用 | | | |

⚠ `predict.py` `predict_nk.py` は**紐2のまま**。紐1で運用するなら**表の紐2頭のうち上位1頭だけ買う**。
**再学習は頻繁にやらなくてよい**（(93)。3ヶ月ごと−12ヶ月ごと = −0.35pt CI[−3.40,+2.61]）。

### スコア表は必ずこの9列（`ml/score_table.py` が唯一の実装）
```
順  馬番  馬名  枠  複勝圏内確率  レース内シェア  市場  差  単勝
```
`predict.py` / `predict_nk.py` / `reco_table.py` が共有。**列を勝手に足さない・減らさない**。

---

## 8. ブランチ

作業ブランチ: `claude/handoff-env-check-2kexpo`（並行セッションは `claude/new-session-h4ydgl`）。
**作業ツリーは切り替えず `git worktree` で同期すること**（並行セッションが同じディレクトリを使う）。
