# 引き継ぎメモ（新しいセッションの最初に貼る用）

`HANDOFF.md` が本体（3,400行）。**これは「まず何を読み、何をやり、何をやらないか」だけの案内**。

---

## 0. まずこれを言う（新セッションへの指示文）

> 競馬の予想モデルのプロジェクト。作業ディレクトリは `keiba-index-generator`。
> **まず `HANDOFF.md` の「最初に読む3行」「★判定基準」「次にやること」を読んで**。
> 90回以上の検証を積んでいるので、**同じ検証をやり直さないこと**。
> 「効かないと確定したもの」に載っている方向には戻らない。
> 数字を報告する前に、必ず判定基準11項目を当てること。

---

## 1. 今どこにいるか（3行）

1. **儲からない（最良85%）。これは覆っていない。** 90回超の検証で一度も100%を超えていない。
2. **(89)で「上界」が出た。** 単勝オッズを使う**あらゆる買い方**の1レース対数成長率は
   `log(払戻率) + D` を超えられない（ケリーが上限）。実測で枠連 −21.4% / 三連単 −33.7%。
   → **買い方・点数・軸紐・資金配分の探索はもう不要**。信号を増やす以外に道は無い。
3. **(90)で機械学習の方向も閉じた。** 現行モデルの D は**全5券種で市場に負けている**
   （枠連 モデル −0.0693 vs 市場 +0.0153、差 −0.0846 [−0.0915,−0.0777]）。
   **符号が逆なので改善では届かない**。以後は **q に何を入れるかの勝負であって、機械学習の勝負ではない**。
   ★皮肉だが**モデルを外した「単勝オッズだけ」の方が強い**。
4. **(94)で最後の候補にも天井が出た。** 「発走直前に単勝1.6倍以下の馬の複勝」は
   **確定オッズを知っているオラクルでも98.2%**（9帯から最良を選んだ後の値。年代で割ると保たない。
   安定するのは≤1.3倍の97%前後）。**複勝の最低配当100円が天井を作っている**
   （1.1-1.2倍では的中の71%が100円配当＝利益ゼロ・的中率は95%止まり）。
   → **これは「儲かる候補」ではなく「複勝ベタ買いより13pt良い」という記述**。

5. **(96)〜(106) で「儲からない」の中身が組み替わった（2026-08-06）**。結論（85%止まり）は不変だが:
   - **測り方が間違っていた2件**: (96) 確率の変換式（Harville）に系統誤差。「市場が下手」と思っていた
     分の**半分以上がこちらの計算ミス**だった。(99) 層別した複勝のDは**原理的に計算できない**
     （複勝の払戻は着内に来た馬にしか公表されないので、層別すると「来なかった場合」が抜ける）。
   - **(100) モデルは市場の**上に**情報を積んでいた**（プラセボ通過・プロジェクト初の直接証拠）。
     「モデルを捨てた方が強い」は**単体で使えば、に限定して訂正**。
   - **(101) レース選択は閉じた**。完全オラクルは必要量を超える＝**儲かるレースは実在する**が、
     事前情報からの**予測の相関が 0.000〜0.008**。「あるか」ではなく「見分けられるか」だけが問題。
   - **(105) 「妙味の無い組は買われない」は市場全体の性質**。7券種すべてで単調。

### D（対数スコア差）という物差し — これが今の主役
```
D = E[log q] − E[log q_pool]     成長率 = log(払戻率) + D
★儲かる条件は一撃で判定できる:  D ≥ |log(払戻率)|
   必要量: 複勝/単勝 0.2231 / 枠連・馬連 0.2549 / 三連複 0.2877 / 三連単 0.3216
   現状:   単勝オッズのHarville +0.0145（必要量の5.7%）／現行モデル 負
```
**新しい信号は、まずDで測る**。ROIで比較すると測定限界に負ける（枠連で1ptの検出に50年分）。
⚠**(99)でDの例外が1つ見つかった**: **すべての結果が観測される場合にしかDは使えない**。
　複勝は着内馬の払戻しか公表されないので、**馬の属性で層別したDは上方バイアスを持つ**
　（1番人気は63%しか来ないのに、来た63%だけを見ることになる）。層別は**実測ROI**で見ること。

★**qの作り方は(96)(97)(100)で更新された。今の最良形**:
```
1. 市場 p_mkt = 正規化した1/オッズ                      … 土台。単体でこれが最強
2. モデルを14%混ぜる  q ∝ p_mkt^0.86 · p_model^0.14     … (100)。プラセボ通過
3. 2着・3着の段に λ を当てる（Harvilleの系統誤差の補正） … (96)
   市場側 λ2≈0.85 / λ3≈0.72 ／ モデル側 λ2≈0.68 / λ3≈0.56（モデルは過信が強い）
   1着の段は τ≈1.0 で補正不要（単勝プールに favourite-longshot bias は無い）
```
　これで枠連 D = +0.0199（必要量0.2549の**7.8%**）。混合比をレースごとに変えても利得ゼロ((104))。

Dは1レース1標本なので桁違いに精密（枠連でCI幅0.008）。実装は `ml/audit_crosspool3.py` の
`collect()` の q を差し替えるだけ。

---

## 2. 環境の再構築（クラウドは毎回まっさら）

```bash
pip install lightgbm scikit-learn pandas numpy joblib
python3 ml/train_prod.py          # ★実運用モデルを作る（約2分）
```
- `ml/model_prod/` は **.gitignore なので新環境では空**。作らないと `predict.py` が動かない。
- `train.py` が作る `ml/model/` は**オッズなし・win目標の旧構成＝別物**。
- `train_prod.py` が **枠連84.5% / 三連複BOX4 84.5%** を表示すれば環境は正常。

### 実行場所の分担（再検討しないこと）
**netkeibaはクラウドから取得できない**（プロキシがCONNECTを403で拒否。設定で回避できる類ではない）。

| 作業 | 場所 | 依存 |
|---|---|---|
| 取得 `nk_fetch.py` / 名寄せ `nk_link.py` / 答え合わせ `nk_score.py` / 表 `reco_table.py` | **Mac** | 標準ライブラリのみ |
| **予想 `predict_nk.py` / 学習 `train_prod.py`** | **クラウド** | pandas/numpy/lightgbm |

★**Macにpandasは入っていない**。取得系に依存を足さないこと。

```bash
# 週次（Mac）
python3 ml/nk_fetch.py results 20260802
python3 ml/nk_link.py
python3 ml/nk_fetch.py pedigree
# 当日朝（Mac）※発売前はオッズAPIが空を返すので朝に取り直す
python3 ml/nk_fetch.py entries 20260809
# 予想（クラウド）
python3 ml/predict_nk.py --date 20260809 --out data/reco/reco_20260809.json
```

---

## 3. 守る作法（★これを飛ばすと必ず偽陽性が出る）

新しい主張をする前に `HANDOFF.md` の「★判定基準」11項目を読む。要点だけ:

1. **1シードの結果を報告しない**。学習の乱数だけで部分集合ROIは61.7%↔116%動く。最低3シード、判断は5シード。
2. **必ず「人気順で同じ買い方をした対照」を並べる**。ベタ買い基準の数字は+10pt過大に見える。
3. **プラセボ対照（同じ一致率のノイズ順）も並べる**。中身が空でも +1.4pt 出る。
4. **単一分割ではなくウォークフォワード**（各年をそれ以前の全データで学習）。三連複が2.3pt動いた。
5. **買い方どうしの比較はROIでなく「1レース期待損失（円）」**。ROI差は検出限界未満で測れない。
6. **指標・閾値・判定条件は測る前に宣言する**。最良のビンで判定しない。**曲線の単調性（Spearman ρ）で見る**。
7. **絞ると測れなくなる**。全体なら±4.5ptで2年、絞ると±44.6ptで199年かかる。
8. **報告するROIには±4ptの設定不確実性が乗る**（ハイパラでROIは78.6〜86.1%に散る。事前に選べない）。
9. **★(94) 数字をHANDOFFに書くならスクリプトを同じコミットに入れる**。(92)はその場限りの集計だったので
   同じ対象の頭数が②2,466・③2,843と食い違ったまま残り、誰も検算できなかった。

---

## 4. もう戻らないこと（すべて実測で否定済み）

- 特徴量の追加（血統・馬場・母の半兄弟・レース内相対 …）— **オッズが全部吸収する**（6例以上）
- ハイパーパラメータ最適化 / 較正 / 層別（場・馬場・血統・クラス）
- **買い方の探索**（8券種29通り横断済み。(89)④の上界で一括否定）
- **指数での選別・絞り込み**（(84)(85)(86)。Dの上位区間は5条件中0件通過）
- **残差学習・市場との差分学習**（(75)。残差のみの順位付けは AUC 0.3605＝逆相関）
- **小倉が良い**（(73)+14.4pt → (76)(91)で消滅。Dで見ると +0.0147 で全体 +0.0145 と同じ）
- **季節・夏競馬で分ける**（(91)。夏は全5券種で全体より下。α=0.05/18を通った区分はゼロ）
- **薄いプール（ローカル開催）ほど市場が雑**（(91)。**向きが逆**で、5券種すべてでローカルの方が低い）
- **前日の馬場傾向**（(74)。傾向は残るが買い目に使うと悪化。プラセボで93.6%が「有利」判定）
- **容量L5が良い**（(81)〜(83)。5シードで+1.34pt・CIが0をまたぐ。L2据え置き）
- **Dの上位区間＝市場の見落とし**（(87)。実際は市場の誤り+0.1pt / モデルの誤り+26.5pt＝**モデルが間違えている**）

---

## 5. 次の宿題（この順で）

### ~~1. 時系列オッズを数十開催ぶん集める~~ → **(94)で格下げ（2026-08-06）**
**(94)の結論**: この候補には**天井があり、それは100%より下**。`python3 ml/odds_ts_place.py` で再現できる。

| 選択 | 事前に使えるか | 複勝ROI | 線(80%)との差 | 標本 |
|---|---|---|---|---|
| 確定 1.0≤o<1.6倍 | ✕ 事後 | 94.9% | +14.88pt | 2,843頭 |
| **確定 1.6倍以下（★実際のルール）** | ✕ 事後 | **93.2%** | **+13.24pt** | 3,977頭 |
| **確定 1.3-1.4倍（★14年で最良の帯）** | ✕ 事後 | **98.2%** [95.6,100.7] | +18.2pt | 490頭 |
| 発走10分前に1.6倍以下 | ○ | 96.3% | +16.3pt | **41頭**（判定しない） |
| 1番人気（人気順位で選ぶ） | ○ | 84.1% | +4.09pt | 42,181頭 |

- ★**「発走n分前で選ぶ」は「確定オッズで選ぶ」のノイズ版**なので、確定オッズ選択が一族の上限。
  その上限が **9帯から最良を選んだ後でも98.2%** ＝ **100%に届かない**。
- 機構: **複勝の最低配当100円が天井を作る**。1.1-1.2倍では**的中の71%が100円配当＝利益ゼロ**、
  的中率は94.7%止まり → 97.5%。詰めるほど払戻が下限に張り付く。
  ⚠(92)①「下限は優位の正体ではない」は正しい。**下限は優位を作っていない、頭打ちにしている**。
- ⚠(92)⑤の「10分前96.4% > 確定93.3%」は**361レースで測り直すと向きが再現しない**（確定の方が高い）。
- ⚠(92)②の表（2,466頭 94.5%）は**誤り**。(92)③の2,843頭が正しく、(94)が再現した。
- → **集める価値は残るが、それは下の2の材料としてであって、この候補の裏取りとしてではない**。
  集めた先にあるのは「複勝ベタ買いより13pt良く、控除率には7pt足りない」の精度向上。

### 1. オッズの時系列変化を「特徴」に入れる（★(94)で繰り上げ・最優先）
`data/odds_ts/` は**買うタイミングの検証にしか使っていない**。
**「オッズがどう動いたか」＝市場自身が出す情報（直前に売れた馬）は一度も特徴として試していない**。
競馬の実証研究で最も再現性がある系統。**単勝オッズの外にある数少ない情報**。→ **Dで測る**。
★**(94)以後、時系列オッズを集める意味はここに一本化された**。問いは
「**発走前オッズは確定オッズに無い情報を持つか**」——持たないなら(89)④の上界がそのまま効く。
⚠現在 `data/odds_ts/` は361レース（11開催日）。**Dで測るには足りない**ので、
**TARGETからの追加出力（手順は`HANDOFF.md`(65)）は引き続きユーザー側のMac作業として必要**。

### ★100%までの距離（2026-08-06時点・枠連）
| q の作り方 | D | 必要量(0.2549)に対して |
|---|---|---|
| Harville・市場のみ（本セッション開始時） | +0.0142 | 5.6% |
| λ補正（(96)で変換の系統誤差を修正） | +0.0182 | 7.1% |
| ＋モデルを14%混合（(100)） | +0.0199 | 7.8% |
| ＋L2-top3 も混合（(102)） | +0.0202 | 7.9% |
★**測り方を直して混ぜて 5.6%→7.9%**。残り92%は**この方向の積み上げでは埋まらない**。
★実測側の到達点は**複勝96.8%**（(106)・99%CIが100%を除外）。機構も判明していて
　**最低配当100円の床の中にいる限り `的中率×100円` を超えられない**。
→ **100%に届く道は「単勝オッズの外にある情報を増やす」しか残っていない**。

### 2. ★容量・特徴の違うモデルを混ぜる（(102)で方向が絞れた）
　(102)で **L2-top3 の重みは0.009＝ほぼ効かない**と判明。**同じ特徴・同じ容量なら、
　目標を変えても勝率推定に足せる独立な情報はほとんど無い**。
　→ 混ぜて稼ぐなら**容量か特徴を変える**必要がある。L5（容量が違う）を検証中。
　★**次に試すなら「特徴の一部を落としたモデル」**（例: 近走成績だけ／血統だけ）。
　　わざと情報を制限すると**違う誤り方**をするので、混合の相手として価値が出る可能性がある。

### 3. シード間の不一致を「除外」に使う（(95)で決着済み・記録のみ）
**(95)で実施済み**。cv（1位馬確率のシード間ばらつき）で除外すると単調に効き、
交絡対照（モデル自身の1位確率・市場の1位確率）を5/5で上回った。
★だが**差は0を超えず**（埋まったのは17%）、**(104)で混合比に変えても利得ゼロ**。
　→ **この筋は閉じた**。順位の入れ替わり(top1_flip)は83%が全シード一致で使えない。

### 4. 枠連の発走前オッズを集める
(89)で枠連プールだけ D>0 だったが、「枠連プールが甘い」のか「Harvilleが枠集約で誤差を相殺しているだけ」
なのかを現データでは分離できない。netkeibaのオッズAPIは枠連も返す。
⚠**分離しても上界は動かない＝実利は無く記述の精度が上がるだけ**。1・2より優先しない。


---

## 6. 現行の運用（変えていない）

| 目的 | 買い方 | 1R期待損失 | コスト | ROI |
|---|---|---|---|---|
| **本命** | **枠連 軸枠×紐枠1**（軸馬の枠 × モデル2位馬の枠・**1点**） | 14.8円 | 100円 | 85.2% |
| **対抗（測定が最も確か）** | **複勝 top1**（CIが3倍狭い） | 15.2円 | 100円 | 84.8% |
| 買うな | 三連複BOX4（+42.9円余計に失う）/ 三連単 / 9頭立て以下 / 券種の併用 | | | |

⚠ `predict.py` `predict_nk.py` は**紐2のまま**。紐1で運用するなら**表の紐2頭のうち上位1頭だけ買う**。

**再学習は頻繁にやらなくてよい**（(93)。3ヶ月ごと − 12ヶ月ごと = −0.35pt CI[−3.40,+2.61]）。
`train_prod.py` は環境を作り直したときに1回走らせれば足りる。

### スコア表は必ずこの9列（`ml/score_table.py` が唯一の実装）
```
順  馬番  馬名  枠  複勝圏内確率  レース内シェア  市場  差  単勝
```
`predict.py` / `predict_nk.py` / `reco_table.py` が共有。**列を勝手に足さない・減らさない**。

---

## 7. ブランチ

作業ブランチ: `claude/handoff-env-check-2kexpo`（並行セッションは `claude/new-session-h4ydgl`）。
両者は随時マージして同期している。**作業ツリーは切り替えず `git worktree` で同期すること**
（並行セッションが同じディレクトリを使っている）。
