"""(231) ★★★**該当0本の開催日は何%か** —— 軸の頻度を「レース」ではなく「日」で数える

★★**動機（2026-09-08・利用者の指定「はかって」）**
　★**ANA_SNS_BRIEF.md は「5日に2日（39.1%）は該当0本」と書いている**が、
　⚠**リポジトリ内にこの39.1%を出した測定が見つからない**。**1日あたり1.2本までしか無い**。
　★**SNSの体感は「1日1.2本」ではなく「今日は買えるのか / 何日当たっていないか」で決まる**
　　ので、**日単位の分布が要る**。

■ ★★★**これは検定ではない。★純粋な記述である**
　★**新しいマスを1つも作らない**——**券種も紐も点数も出てこない**。
　★**(210)で凍結した軸判定の出力を、レース単位から日単位に組み替えるだけ**。
　⚠**だから「良い数字が出た」という読み方をしてはいけない**（判定基準43）。

■ ★軸（**(210)で凍結・動かさない**）
　**pn ≥ 0.15 かつ ズレ ≥ 0.15 かつ 単勝オッズ ≥ 10.0倍 の中で pn 最大の1頭**。
　★**候補が無ければ見送り**。⚠**買い目は複勝1点のみ**（**見せ場の設計はこの測定の外**）。

■ ★★★**分母の定義（★先に決める・ここを曖昧にすると数字が動く）**
| 記号 | 分母 | ★**意味** |
|---|---|---|
| **(a)** | ★**判定が走った日** | **板が引けて軸判定まで到達したレースが1本以上ある開催日** |
| **(b)** | ★**開催日ぜんぶ** | **モデルの予測が出たレースが1本以上ある開催日** |
　★**(a)は「軸が本当に立たなかった日」**、**(b)は「利用者が朝に画面を見る日」**。
　⚠★**(b)の0本には「板が無くて判定できなかった日」が混ざる**——
　　**両方出さないと必ず誤読する**（**判定基準26: 物差しを替える**）。

■ ★出す量（**4つ**）
　1. **開催日ごとの該当本数の分布**（**0 / 1 / 2 / 3本以上**）
　2. ★**該当0本の日の割合** ← ★**ブリーフの39.1%と突き合わせる**
　3. **複勝が1本以上当たった日の割合**
　4. ★★**連続で当たらない開催日数**（**中央 / 95%点 / 最大**）← ★**SNSの体感そのもの**

■ ★★ゲート2（判定基準42）—— **この測定は何を返せば「意味なし」か**
　★**記述なので偽の返り値は無い**。⚠**だから対照を1つ置く**:
　★★**同じ日付集合の上で、的中フラグだけを無作為に並べ替えた場合の連続外れ日数**を併記する。
　★**的中が日にランダムに散っているだけなら、実測はこの対照と一致する**。
　★★**一致しなければ「当たりが日に固まっている」＝★体感はこの数字より悪い**。
　⚠**並べ替えは該当本数の構造を壊さないよう、★日ごとの該当本数は固定したまま
　　「その日に当たったか」だけを入れ替える**（**NRAND=10種・SEEDは(209)と同じ**）。

■ ⚠⚠★★★**訂正2（2026-09-08・★対照の書き方が間違っていた。判定基準37）**
　★**素朴な並べ替え（対照1）は、★的中フラグを「該当0本の日」にも配ってしまう**。
　⚠★**該当0本の日は、定義上ぜったいに当たらない**。**そこに的中を置ける対照は、
　　的中を実際より均等にばらまく＝★外れの連を実際より短く見せる**。
　　→ ★★**実測が対照より長く出るのは当たり前で、これは腕の手柄ではない**。
　★★**正しい対照（対照2）: その日の該当本数 n_d を保ったまま、
　　1本ごとに p=23.4% で独立に当たるとして引き直す**。
　　★**これなら「0本の日は当たらない」という構造が対照にも入る**。
　★**両方を出す**——⚠**対照1は「私が最初に書いた間違った対照」として残す**
　　（**判定基準37を5回踏んだ記録がある。★6回目を隠さない**）。
　■ ★**判定はどちらで行うか: ★対照2だけで行う**。**対照1は参考にしない**。

■ ★★内部対照（**⚠これが合わなければ経路が違う。結果を読まない**）
　★**該当レースの総数が 1,383本**（**(218)の参考行・複勝1点の本数**）**±5**。
　★**的中率が 23.4% ±0.5pt**。★**軸の中央オッズが 15.9倍 ±0.5**。

■ ⚠⚠★★★★**訂正（2026-09-08・★私が判定基準37を踏んだ。★この項目で6回目）**
　★**本数の対照が「落ちた」: 1,398本 vs 1,383本**。⚠★**落ちていなかった。★対照の書き方が間違っていた**。
　★★**1,383本は「乱が10種とも引けたレースだけ」という★条件つきの数**であり、
　　★**私が数えた1,398本は条件なしの全数**。⚠**別の量を比べていた**。
　★**`audit_ana_bet.py` を回すと、同じログに両方が出る**:
| 出力 | 値 | 中身 |
|---|---|---|
| `★対象 **1,398レース**` | **1,398** | ★**条件なしの全数**（`len(fix)`）＝**私が数えたもの** |
| 全マス表の複勝の本数 | **1,383** | ★**`if not okd: continue` を通った後**＝**乱が引けたレースだけ** |
　★★**つまり私の経路は最初から正しく、1,398で厳密に一致していた**。
　⚠★★**(211)の初版もまったく同じ間違いをしている**（**「全数1,398本と比べた」・判定基準37・5回目**）。
　　★**私はその記録を読んでいながら、同じ場所で同じ穴に落ちた**。

■ ⚠⚠★★**取り消した誤りの記録（★消さずに残す）**
　★**私は一度「これは環境差で、(211)の189マスはこの環境では再現できない」と書いて
　　ANA_TRACK.md にも入れた**。⚠★**これは誤りだった**。**根拠として挙げたものは全部本当だが
　　（板は増えていない・予測は run-to-run で完全一致・キャッシュは .gitignore・版の固定なし）、
　　★肝心の「本数が合わない」の理由が環境差ではなかった**。
　★★**`audit_ana_bet.py` は当環境で内部対照を厳密に再現する**——
　　**L=10・複勝 102.1%・1,356本、復元R 0.8188、三連複BOX4 81.9%、全マスの数字も凍結値と一致**。
　　→ ★★**再現性の穴は存在しない。私の読み違いだった**。
　⚠**ただし「予測キャッシュが .gitignore で版の固定も無い」こと自体は事実で、
　　★将来ライブラリが変われば本当に再現できなくなる**。**それは別の話として残す**。

■ ⚠**下の訂正は★訂正3（2026-09-08）で取り消されている**——**「環境差」は誤診で、真因は1,383と1,398の取り違えだった**。
　★**消さずに残す**（**判定基準37を踏んだ記録**）。下の「訂正」は★当時の書き方である。

■ ⚠⚠★★★**訂正（2026-09-08・★日別の結果を1つも見ていない段階で直している）**
　★**本数の対照が落ちた: 1,398本 vs 1,383本（+15・+1.1%）**。
　★**判定基準37に従い、まず対照の書き方を疑って調べた**。**分かったことを全部書く**:
| 調べたこと | 結果 |
|---|---|
| **板が増えたか** | ⚠**増えていない**。**data/ への最後のコミットは 09-07 08:45 で、(218)の測定 09-07 15:13 より前** |
| ★**予測は run-to-run で再現するか** | ★**再現する**。**457,465行が完全一致・最大差 0.000e+00** |
| ★**凍結時の予測キャッシュは残っているか** | ⚠★**残っていない**。**`data/cache/` は .gitignore されており、当時のコンテナと共に消えた** |
| **ライブラリの版が固定されているか** | ⚠**されていない**。**requirements も pyproject も無い**（**今: lightgbm 4.7.0 / numpy 2.4.6 / pandas 3.0.5**） |
　★★**結論: これは経路の誤りではなく★環境差である**。**同じコードと同じデータでも、
　　別のコンテナで学習したモデルは境界のレースを15本ぶん違って拾う**。
　⚠★★**つまり(211)の189マスは、この環境では厳密には再現できない**。
　　★**これは私の測定の問題ではなく、★プロジェクト全体の再現性の穴である**（**下に書いた**）。
　★**的中率(23.5% vs 23.4%)と軸の中央オッズ(15.8 vs 15.9)は通っている**
　　＝**母集団の性格は同じで、境界の15本だけが違う**。

　★★**そこで対照をこう書き直す（★理由を先に書く）**:
　★**本数は「1,383〜1,398の範囲に入ること」を対照にする**（**凍結値と当環境の実測値の両端**）。
　⚠**これは緩めているので、★代わりに日別の答えに誤差の上界を付ける**——
　　★**15本のレースは高々15日を「0本の日」から動かせない**ので、
　　★**0本の日の割合の不確かさは ±15/日数 で厳密に抑えられる**。**それを併記する**。
　⚠**的中率と中央オッズは緩めない**（**通っているものを緩める理由が無い**）。

■ 予想（⚠**当てにしない**・判定基準24。★**前セッションの私は10回外した**）
　★**(a)の0本の日は 35〜45%**と見る（**ブリーフの39.1%が正しければここに入る**）。
　★**(b)はそれより高い**と見る（**板の欠けが混ざるため**）。
　★★**連続外れの最大は20開催日を超える**と見る（**年104日なので2ヶ月以上**）。
　⚠**もし実測が対照より明確に長ければ、それは「当たりが日に固まっている」ことを意味する**。


■ ★★★★**結果（実測済み・2026-09-08）**
　★**内部対照3つとも通過**（**1,398本 / 的中率 23.5% / 軸の中央 15.8倍**）。
　⚠★**本数は `audit_ana_bet.py` の『★対象 1,398レース』と厳密に一致**（**1,383本は乱が引けたレースだけの条件つきの数。★比べてはいけない**）。

**★①開催日ごとの該当本数**（**(a) 1,142日 / (b) 1,144日**）
| 該当 | (a) 日数 | (a) 割合 | (b) 日数 | (b) 割合 |
|---|---|---|---|---|
| ★**0本** | **447** | ★★**39.1%** | **449** | **39.2%** |
| 1本 | 344 | 30.1% | 344 | 30.1% |
| 2本 | 170 | 14.9% | 170 | 14.9% |
| 3本以上 | 181 | 15.8% | 181 | 15.8% |
| **平均** | **1.22本** | | **1.22本** | |
　★★**ブリーフの「5日に2日（39.1%）は該当0本」は★厳密に正しかった**（**447/1,142 = 39.14%**）。
　★**(a)と(b)がほぼ同じ**＝**板の欠けは2日しかない**。⚠**私が心配した誤読の余地は無かった**。

**★②複勝が1本以上当たった日: 271日 / 1,142日 = ★23.7%**
　★**「4日に1日」当たる**。⚠**買えるのは60.9%の日だけなので、
　　★買った日に限れば 271/695 = 39.0%**（**買った日の5日に2日**）。

**★★③連続で当たらない開催日数: 中央 2日 / 95%点 14日 / ★最大 35日**

■ ★★★★**ゲート2 —— ⚠私は対照の書き方をまた間違えた（判定基準37・★6回目）**
| 対照 | 中央 | 95%点 | ★最大 | 95%区間 | ★**実測以上の割合** | 判定 |
|---|---|---|---|---|---|---|
| ⚠**対照1（素朴・間違い）** | 2.0日 | 10.4日 | **22.2日** | 16〜33日 | ⚠**0.015** | ⚠**実測の方が長い** |
| ★**対照2（正しい）** | 1.2日 | 13.4日 | ★**42.3日** | ★**27〜71日** | ★★**0.738** | ★★**区別がつかない** |
　⚠★**対照1は的中フラグを「該当0本の日」にも配っていた**——**0本の日は定義上当たらない**ので、
　　**的中を実際より均等にばらまき、外れの連を短く見せる**。→ ★**実測が勝つのは当たり前**。
　★★**正しい対照2では p=0.738 で完全にひっくり返る**。**対照2の最大は平均42.3日で、
　　★実測の35日より★長い**。→ ★★**当たりは日に固まっていない**。
　★★**結論: 最大35日の空白は、的中率23.4%と「0本の日が39.1%」から出るただのばらつきである**
　　（**判定基準43**）。⚠**SNSの体感が測定より悪い、ということは無い**。

■ ★★予想の答え合わせ（判定基準24・★**このセッションの1回目**）
| 予想 | 実際 | |
|---|---|---|
| **(a)の0本の日は 35〜45%** | ★**39.1%** | ★**当たり** |
| **(b)はそれより高い** | ⚠**39.2%（ほぼ同じ）** | ⚠**外れ**（**板の欠けは2日しかなかった**） |
| **連続外れの最大は20開催日を超える** | ★**35日** | ★**当たり** |
　⚠★**ただし「当たり」の2つは、対照2に照らせば★偶然でも同じ値が出る**。**手柄ではない**。

■ ⚠⚠★★★★**この測定でいちばん高くついた誤り（★私が判定基準37を踏んだ・6回目）**
　★**内部対照が落ちたとき、私はそれを「環境差」と診断した**——
　　**「同じコード・同じデータでも本数が1,383→1,398に動く。(211)の189マスは
　　この環境では厳密に再現できない」**と書き、★**ANA_TRACK.md にも入れて利用者にも報告した**。
　⚠★★**これは誤りだった**。
　★**`audit_ana_bet.py` を実際に回したら、対照も全マスも凍結値どおりに再現した**:
| 対照 | 結果 |
|---|---|
| **L=10・複勝** | ★**102.1% / 1,356本**（**(210)と厳密に一致**） |
| **復元R** | ★**0.8188** |
| **三連複BOX4** | ★**81.9%** |
| **P複勝1点** | ★**1,383本 / 101.5% / 23.4%**（**凍結値と一致**） |
　★★**本当の理由は「別の量を比べた」**——**1,383は乱が引けたレースだけの条件つきの数**、
　　**1,398は条件なしの全数**。★**同じログに両方が出ている**。
　⚠★★**(211)の初版がまったく同じ間違いをした記録を、私は読んだうえで同じ穴に落ちた**。
　★**教訓（★この項目の書き方を変える）**: ⚠**「対照が落ちた」と思ったら、
　　★まず対照の値がどう作られたかをソースで確かめる**——
　　**ドキュメントの数字を引き写して比べると、条件つきかどうかが落ちる**。
　★**「調べた根拠」が全部本当でも、診断は間違いうる**
　　（**板は増えていない・予測は完全に決定的・キャッシュは .gitignore・版の固定なし
　　——全部事実だが、本数が合わない理由ではなかった**）。

■ ★**それでも残る本物の指摘（★上とは別の話）**
　⚠**`data/cache/wf_pred.npz` が `.gitignore` されていて、版の固定も無い**のは事実。
　★**今は再現するが、ライブラリが上がった時点で再現しなくなる**。
　⚠★**ただし ANA_RULE.md の凍結とは別の話なので、勝手には直していない**。

実行: python3 ml/audit_ana_days.py
"""
import os
import sys
from binascii import crc32

import numpy as np

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

import features as F
from audit_crosspool import load_races, payoff
from audit_ana_odds import MIN_HORSES, band_of, BANDS
from audit_ana_marg import wf_predict
from audit_ana_board import NPLACE, load_fuku_boards, qpool
from audit_ana_band import PN_FLOOR
from audit_ana_hole import GAP, NRAND, SEED

# ★★(244sns) 既定は(231)のまま。★フラグを付けたときだけ SNS 版の条件で測る
#   `--gap 0.10`   … ズレの下限を変える（**(240sns)でSNS側が採用した水準**）
#   `--no-jump`    … 障害戦を落とす（**(243sns)**）
#   ⚠**既定（フラグ無し）の出力は(231)と1文字も変わらない**
GAP_OPT = GAP
NO_JUMP = False


def _is_jump(dist):
    """★(243sns)の判定: 距離が100の倍数でない、または3600m超"""
    try:
        v = float(dist)
    except (TypeError, ValueError):
        return False
    return v > 0 and (v % 100 != 0 or v > 3600)

# ★★(231b) 並べ替えの回数を 10 → 1,000 に増やす（★2026-09-08・日別の結果を見た後）
#   ⚠**これは腕を変えていない。★事前登録した「実測の最大が対照より明確に長いか」を
#     判定するには、対照の★ばらつきが要る**——**10種では平均しか出せなかった**。
#   ★**対照を強くしただけで、実測の側は1つも動いていない**。
NSHUF = 1000
from audit_ana_fix import LFIX
from train_prod import add_odds_features

# ★★条件なしの全数と比べる（audit_ana_bet.py の `★対象 N レース` と同じ量）。
#   ⚠**1,383本は「乱が引けたレースだけ」の条件つきの数なので、ここでは使わない**（★上の訂正）。
KNOWN_N, N_TOL = 1398, 0
KNOWN_HIT, KNOWN_ODMED = 23.5, 15.8
HIT_TOL, OD_TOL = 0.5, 0.5


def runs_of_misses(hits):
    """★**当たった日と当たった日の間に挟まる「当たらない開催日」の本数**を並べる。
    ⚠**先頭の連（最初の的中より前）と末尾の連は★打ち切られている**ので落とす
    （**判定基準43: 端の1標本を結論に混ぜない**）。"""
    idx = [i for i, h in enumerate(hits) if h]
    if len(idx) < 2:
        return []
    return [b - a - 1 for a, b in zip(idx, idx[1:])]


def describe(runs):
    if not runs:
        return "—"
    a = np.array(runs, float)
    return (f"中央 {np.median(a):>4.0f}日 / 95%点 {np.percentile(a, 95):>4.0f}日 / "
            f"最大 {a.max():>4.0f}日　(連 {len(a)}本)")


def main():
    global GAP_OPT, NO_JUMP
    for i, a in enumerate(sys.argv):
        if a == "--gap" and i + 1 < len(sys.argv):
            GAP_OPT = float(sys.argv[i + 1])
        if a == "--no-jump":
            NO_JUMP = True
    tuned = (GAP_OPT != GAP) or NO_JUMP
    print("(231) ★★★**該当0本の開催日は何%か** —— 軸の頻度を「日」で数える")
    if tuned:
        print(f"★★**(244sns) SNS版の条件で測る**: "
              f"**ズレ下限 {GAP_OPT}**"
              + ("　★**障害戦を除外**" if NO_JUMP else "")
              + "　⚠**内部対照は(231)の値なので落ちる。★それが正常**")
    print("★**これは記述であって検定ではない**。⚠**新しいマスは1つも作っていない**\n")

    races = {r["rid"]: r for r in load_races()}
    print("★複勝の板(type=2)を読む…")
    boards = load_fuku_boards()
    print(f"　**{len(boards):,}レース分**")

    d = F.to_model(F.load_files())
    f = F.build_features(d)
    keep = (f["n_prior"] >= 1) & d["odds"].notna() & (d["odds"] > 0)
    d, f = d[keep].reset_index(drop=True), f[keep].reset_index(drop=True)
    y = (d["finish"] <= 3).astype(int).to_numpy()
    fx, _ = F.encode_categoricals(f)
    fx = add_odds_features(fx, d["odds"].to_numpy(float), d["raceid"].to_numpy())
    pred = wf_predict(d, fx, y, 3)
    msk = ~np.isnan(pred)
    sub = d.loc[msk, ["raceid", "umaban", "odds", "date", "distance"]].copy()
    sub["p"] = pred[msk]

    # ★日ごとに: ran = 判定が走ったレース数 / cand = 該当本数 / hit = 複勝的中本数
    day = {}
    axod = []
    for rid, g in sub.groupby("raceid"):
        rid = str(rid)
        dt = g["date"].iloc[0].date()
        rec = day.setdefault(dt, {"seen": 0, "ran": 0, "cand": 0, "hit": 0})
        rec["seen"] += 1                       # ★(b)の分母: 予測が出たレース
        r = races.get(rid)
        bd = boards.get(rid)
        if r is None or bd is None:
            continue
        nums = {u for u, _, _ in r["horses"]}
        if len(nums) < MIN_HORSES:
            continue
        gg = g[g["umaban"].astype(int).isin(nums)]
        ub = gg["umaban"].astype(int).to_numpy()
        if len(gg) < MIN_HORSES or not all(int(u) in bd for u in ub):
            continue
        od = gg["odds"].to_numpy(float)
        pv = gg["p"].to_numpy(float)
        if not np.isfinite(od).all() or (od <= 0).any() or pv.sum() <= 0:
            continue
        pn = pv / pv.sum() * NPLACE
        qp, _R = qpool([bd[int(u)] for u in ub], "harm")
        gap = pn - qp
        rec["ran"] += 1                        # ★(a)の分母: 軸判定まで到達したレース
        if NO_JUMP and _is_jump(gg["distance"].iloc[0]):
            continue                      # ★(243sns) 障害戦を落とす
        cand = np.where((pn >= PN_FLOOR) & (gap >= GAP_OPT) & (od >= LFIX))[0]
        if not len(cand):
            continue
        i = int(cand[int(np.argmax(pn[cand]))])
        ax = int(ub[i])
        v0 = payoff(r, "複勝", [ax])
        if v0 is None:
            continue
        rec["cand"] += 1
        rec["hit"] += 1 if v0 > 0 else 0
        axod.append(float(od[i]))

    days = sorted(day)
    nc = np.array([day[t]["cand"] for t in days])
    nh = np.array([day[t]["hit"] for t in days])
    ran = np.array([day[t]["ran"] for t in days])

    # ── ★★内部対照（⚠落ちたら読まない） ──────────────────────────
    tot, hits = int(nc.sum()), int(nh.sum())
    hr = 100.0 * hits / tot if tot else 0.0
    odmed = float(np.median(axod)) if axod else 0.0
    ok = [("該当レース総数", tot, float(KNOWN_N), N_TOL, abs(tot - KNOWN_N) <= N_TOL),
          ("複勝の的中率(%)", hr, KNOWN_HIT, HIT_TOL, abs(hr - KNOWN_HIT) <= HIT_TOL),
          ("軸の中央オッズ", odmed, KNOWN_ODMED, OD_TOL,
           abs(odmed - KNOWN_ODMED) <= OD_TOL)]
    print("\n■ ★★**内部対照**（**(210)(218)と一致するか**）")
    for nm, got, want, tol, good in ok:
        rng = f"{KNOWN_N:,} (厳密)" if nm.startswith("該当") else f"{want:.1f} (±{tol})"
        print(f"　{nm:<16}{got:>10.1f} vs {rng:>14}"
              f"　{'★通った' if good else '⚠落ちた'}")
    if not all(g for *_, g in ok):
        if not tuned:
            print("\n⚠⚠**内部対照が落ちた。経路が違う。★結果を読まない**（判定基準32/37）。")
            return
        # ★★(244sns) 条件を変えたのだから対照が落ちるのは当然
        #   ⚠**「経路の誤り」と「条件を変えた」を混同しない**
        print("\n⚠★**対照は落ちているが、★条件を変えたので当然である**"
              "（**(231)の対照は ズレ0.15・障害込みの値**）。★**経路の誤りではない**。")

    # ── ① 日ごとの該当本数の分布 ─────────────────────────────
    ma = ran > 0                                # (a) 判定が走った日
    mb = np.ones(len(days), bool)               # (b) 開催日ぜんぶ
    print(f"\n■ ★**①開催日ごとの該当本数**"
          f"　(a)判定が走った日 **{ma.sum():,}日** / (b)開催日ぜんぶ **{mb.sum():,}日**")
    print(f"{'該当':<8}{'(a) 日数':>10}{'(a) 割合':>10}{'(b) 日数':>10}{'(b) 割合':>10}")
    for lo, hi, nm in [(0, 0, "★0本"), (1, 1, "1本"), (2, 2, "2本"), (3, 99, "3本以上")]:
        sa = ((nc >= lo) & (nc <= hi) & ma).sum()
        sb = ((nc >= lo) & (nc <= hi) & mb).sum()
        print(f"{nm:<8}{sa:>10,}{100*sa/max(ma.sum(),1):>9.1f}%"
              f"{sb:>10,}{100*sb/max(mb.sum(),1):>9.1f}%")
    print(f"{'平均':<8}{nc[ma].mean():>10.2f}{'':>10}{nc[mb].mean():>10.2f}")
    print("\n　★**本数は audit_ana_bet.py の『★対象 1,398レース』と厳密に一致している**"
          "　⚠**1,383本と比べてはいけない**（**あちらは乱が引けたレースだけ**）")
    # ── ② 的中した日 ────────────────────────────────────
    print("\n■ ★**②複勝が1本以上当たった日**")
    for nm, m in [("(a) 判定が走った日", ma), ("(b) 開催日ぜんぶ", mb)]:
        w = (nh > 0) & m
        print(f"　{nm:<20}{w.sum():>6,}日 / {m.sum():>6,}日"
              f"　= ★**{100*w.sum()/max(m.sum(),1):>5.1f}%**")

    # ── ③ 連続で当たらない開催日数 ───────────────────────────
    print("\n■ ★★**③連続で当たらない開催日数**（⚠**端の連は打ち切られているので落とす**）")
    for nm, m in [("(a) 判定が走った日", ma), ("(b) 開催日ぜんぶ", mb)]:
        print(f"　{nm:<20}{describe(runs_of_misses((nh > 0)[m]))}")

    # ── ★★ゲート2: 的中を日に無作為に散らした対照 ────────────────
    print("\n■ ★★**ゲート2の対照**——**的中が日にランダムに散っているだけなら、ここに一致する**")
    print(f"　⚠**日ごとの該当本数は固定したまま、「その日に当たったか」だけを"
          f"入れ替える**（**{NRAND}種**）")
    for nm, m in [("(a) 判定が走った日", ma), ("(b) 開催日ぜんぶ", mb)]:
        base = (nh > 0)[m]
        cnt = nc[m]
        obs = runs_of_misses(base)
        omax = max(obs)
        print(f"　{nm}")
        print(f"　　★実測　　　　　　中央 {np.median(obs):>4.0f}日 / "
              f"95%点 {np.percentile(obs, 95):>4.0f}日 / ★最大 {omax:>3.0f}日")
        for lab, gen in (("⚠対照1(素朴)", "perm"), ("★対照2(正しい)", "binom")):
            med, p95, mx = [], [], []
            for sd in range(NSHUF):
                g = np.random.default_rng([SEED + sd, crc32((nm + gen).encode())])
                if gen == "perm":
                    sim = g.permutation(base)
                else:
                    # ★その日の該当本数 cnt[d] を保ったまま、1本ごとに p で当たる
                    sim = g.binomial(cnt, hr / 100.0) > 0
                r = runs_of_misses(sim)
                if r:
                    med.append(np.median(r)); p95.append(np.percentile(r, 95))
                    mx.append(max(r))
            if not med:
                continue
            mx = np.array(mx, float)
            pv = float((mx >= omax).mean())
            print(f"　　{lab}({NSHUF}種) 中央 {np.mean(med):>4.1f}日 / "
                  f"95%点 {np.mean(p95):>4.1f}日 / ★最大 {mx.mean():>4.1f}日"
                  f"　[95%区間 {np.percentile(mx, 2.5):.0f}〜{np.percentile(mx, 97.5):.0f}日]")
            star = "実測の方が長い" if pv < 0.05 else "★実測と区別がつかない"
            print(f"　　　→ **対照が実測以上の連を出す割合 = {pv:.3f}**　→ **{star}**")

    print("\n★**読み方**: ★**実測の最大が対照の最大より明確に長ければ、"
          "当たりが日に固まっている**＝⚠**SNSの体感はこの数字より悪い**。")
    print("⚠**一致していれば、外れの連は単に的中率23.4%から出るばらつきである**"
          "（**判定基準43**）。")


if __name__ == "__main__":
    main()
