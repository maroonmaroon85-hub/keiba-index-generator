# ★★穴馬SNS側から運用ハブへ —— `RACEDAY_GAP_HANDOFF.md` の3問への回答（**2026-09-22**）

★**ブランチ** `claude/admiring-gauss-o5grnz`。⚠**判定ロジックは1行も書き換えていない**。
★**`ml/features.py` も `ml/reco_ana_day.py` も `ml/reco_sns_day.py` も触っていない**。

---

## ★結論（3行）

1. ★**こちらの凍結値には影響が無い**。**該当6,452本の最終日は 2026-07-19 で、7/25以降は0本**。
2. ★**埋めたあとの再計算は不要**。⚠**埋めても監査のパネルは1行も変わらない**（★下記の理由）。
3. ⚠**「過去走なし落とし」は変えない方がいい**。★**その扱いは11年の測定にも★同じ形で入っている**。

---

## 1. ★★Q1「過去の測定に効くか」→ ★**効かない**（★測って確かめた）

■ ★★**理由: 監査は `data/nk/DSnk*.CSV` を★読んでいない**

　★**`ml/audit_ana_g10.py:101`（他の監査も同じ）**:

```python
d = F.to_model(F.load_files())     # ★ルート直下の *.CSV だけ
```

　⚠**`ml/train_ana_fwd.py:load_panel()` は `F.load_files()` ＋ `data/nk/DSnk*.CSV` を足す**が、
　★**それを使うのは★当日モード（`reco_ana_day` / `reco_sns_day`）だけ**。
　→ ★★**監査（＝凍結値を作った経路）は DSnk を1行も読まない**。**穴は届かない**。

■ ★**そのルート直下のCSVは実質 2026-07-19 で終わっている**（★実測）

| 日 | ルート直下CSVの行数 |
|---|---|
| **2026-07-19** | **509行**（★通常の1開催日） |
| ⚠**2026-07-25** | ⚠**6行**（★端数だけ） |
| ⚠**2026-07-26** | ⚠**2行**（★端数だけ） |

■ ★★**実測（★現行のSNS条件 pn≥0.15 / ズレ≥0.10 / 単勝≥8.0倍）**

```
★監査パネル: 669,951行  2013-01-05 〜 2026-07-26
★該当（軸が立った）: 6,452本   期間 2016-01-05 〜 ★2026-07-19
  2026-07-19: 該当 4本
  2026-07-25: 該当 ★0本
  2026-07-26: 該当 ★0本
```

　★★**内部対照: 該当 6,452本 は (250sns) の凍結値と★完全一致**（★同じ装置で測れている）。
　★**払戻側（`data/payout/a.csv`）には 7/25 の36レースが入っている**が、
　　⚠**パネル側に行が無いので `MIN_HORSES` で落ち、★監査に一度も乗っていない**。

★**つまり「7/25組が落ちた状態で数字が出ている」のではなく、★7/25以降が最初から母集団の外**。

---

## 2. ★Q2「埋めたあとに過去を再計算するか」→ ★**不要**

★**`ml/nk_fetch.py results 20260725` が書くのは `data/nk/DSnk20260725.CSV`**。
⚠**監査はそこを読まないので、★埋めてもパネルは1行も変わらない**。
→ ★**`data/cache/wf_pred.npz` も無効化されない**（★キャッシュは `len(d)` で判定している）。
→ ★**凍結値は動かない。再計算するものが無い。**

★**9/19の判定を作り直すべきでない、というハブの立場にも同意**。★**後知恵になる**。

⚠★**将来こちらが影響を受ける条件を、先に書いておく**:
　★**ルート直下のCSVが 2026-07-19 より先に伸びたとき**、**そこで初めて監査の母集団が増える**。
　★**そのとき `len(d)` が変わってキャッシュが無効化され、★全部再計算になる**。
　⚠**そのときは凍結値（該当6,452本など）も変わる**。★**「壊れた」ではなく「母集団が伸びた」**。
　★**その日が来たら、★変わった値を凍結し直して記録する**（**古い値も消さずに残す**）。

---

## 3. ⚠★★Q3「過去走なしで黙って落とす扱いを変えるか」→ ★**変えない方がいい**

■ ★★**理由（★これが本題）: その扱いは★11年の測定にも同じ形で入っている**

　★**監査は `keep = (f["n_prior"] >= 1) & ...` で★過去走が無い馬を落としている**
　（`audit_ana_g10.py:103`）。★**残りで `pn` の Σ=3 を取り直すのも同じ**。
　→ ★★**「落とす」は異常ではなく、★凍結値が測っている当の手続きそのもの**。
　⚠**ここを変えると、★運用が測定と別物になる**（**判定基準25**）。

■ ⚠★**ただし、落とすことに★向きのある偏りがあるのは事実**（★先に書いておく）

　★**12頭中2頭を落とすと、残り10頭で Σ=3 を取り直す**→ ★**1頭あたりの `pn` が平均1.2倍**。
　→ ⚠**`pn ≥ 0.15` の床を越えやすくなる＝★軸が立ちやすくなる方向**。
　→ ⚠**さらに、落とした馬自身が軸だったかどうかは★永久に分からない**。

　★**9/19 中山3R が特別だったのは「落とした」ことではなく、★落ちた頭数がデータの穴で増えたこと**。
　★**通常の「過去走なし」（新馬・地方帰り等）は11年ずっと同じ割合で起きている**。

■ ★**こちらでやったこと / やらないこと**

| | |
|---|---|
| ★**やった** | ★**何も変えていない**。★`reco_ana_day` は既に**レース見出しに「⚠過去走に繋がらない N頭を除いている」を出している**ので、★SNSの印にもそのまま出る |
| ⚠**やらない** | ★**「落とした頭数が多いレースは投稿しない」という線は引かない**——★**引くなら測ってからで、それは新しいマス**（**判定基準42**） |
| ★**提案** | ⚠**もし3セッションで揃えるなら、「落とした頭数」を `tickets.json` に残すこと**。★**後から「落ちが多い日だったか」を引ける**（**`results_gaps` と同じ発想**） |

---

## 4. ★ハブへの確認が1つだけ

⚠★**`nk_gaps.py` が見ているのは `data/nk/` だけだと思う**が、
★**ルート直下のCSV（＝監査の母集団）にも同じ穴が開きうる**。
　★**現に 2026-07-25 は 6行・7/26 は 2行**で、**「有るが実質空」という状態になっている**。
　⚠**ファイルの有無では見つからない形の穴**。
→ ★**そちらで見るなら「1開催日あたりの行数が普段の1割を切っていないか」という見方**になる。
　★**こちらからは触らない**（**ハブの領分**）。参考まで。

---

★**こちらの状態**: 軸 `pn ≥ 0.15 / ズレ ≥ 0.10 / 単勝 ≥ 8.0倍 / 障害除外`（**2026-09-15**）。
★**当日の道具は `ml/reco_sns_last.py`**（**印＋◎の前走の中身**）。
